# Changelog

## 2026-05-16

- Advanced the thirty-first feature set to 80 / 100 by adding an owner-visible import/export repair loop with reusable linked chart/table repair actions, Backstage copy/download controls, PPTX preflight verification, smoke selectors, and regression coverage.
- Advanced the thirty-first feature set to 60 / 100 by adding a guarded presentation smoke runner bridge with reusable browser-flow execution, a `bun run test:presentation-smoke:bridge` CLI, strict live URL/storage-state/mutation-safe-fixture/approval gates, lifecycle handoff wiring, and regression coverage.
- Advanced the thirty-first feature set to 40 / 100 by adding responsive workspace ergonomics with persisted density and side-panel state, collapsible filmstrip/properties rails, compact toolbar controls, command-palette view commands, stable smoke selectors, and regression coverage.
- Started the thirty-first feature set at 20 / 100 by adding a reusable Native Office package parity report that connects SmartArt/diagram conversion candidates, `.thmx` theme package application paths, master/layout relationship checks, native package writer readiness, PPTX preflight metrics, snapshot grouping, serialization, and regression coverage.
- Completed the thirtieth feature set at 100 / 100 by adding a real app-shell command palette with searchable slide navigation, insert, object, edit, view, and section commands, disabled prerequisite states, a toolbar keyboard trigger, smoke selector coverage, and regression checks; defined the thirty-first feature set around deeper native Office package parity, responsive workspace ergonomics, opt-in smoke execution handoff, advanced import/export repair loops, and presenter delivery polish.
- Advanced the thirtieth feature set to 80 / 100 by adding a shared production fixture lifecycle planner with seeded session metadata, mutation-safe deck reset command reporting, disabled-by-default browser handoff serialization, a `bun run production:fixtures` CLI, production-health/release-handoff integration, and regression coverage for ready and blocked lifecycle states.
- Advanced the thirtieth feature set to 60 / 100 by adding custom motion-path animation offsets and on-click/with-previous/after-previous trigger authoring, including properties-panel and animation-pane controls, slideshow trigger sequencing, native PPTX custom motion-path XML, preflight metrics, speaker-note handoff detail, server/deck normalization, and regression coverage.
- Advanced the thirtieth feature set to 40 / 100 by adding reusable linked-data repair workflows for ODP/Excel linked chart sources, rotated chart editability fallbacks, imported table style gaps, and native table structure handoffs, with PPTX preflight metrics, copyable snapshot output, and presentation regression coverage.
- Started the thirtieth feature set at 20 / 100 by adding an Office theme variant repair report for imported source theme review, `.thmx` package/XML readiness, master-layout variant availability, placeholder inheritance safety, and owner-visible repair actions surfaced in the Master safeguards panel with regression coverage.
- Completed the twenty-ninth feature set at 100 / 100 by adding accessibility and keyboard parity reporting, focus-order and shortcut coverage checks, screen-reader labels for editor landmarks and canvas/layer objects, actionable accessibility diagnostics, visible focus and keyboard-entry checks, and regression coverage; defined the thirtieth feature set around deeper Office theme fidelity, linked-data repair, advanced action/animation authoring, production fixture execution, and app-shell ergonomics.
- Advanced the twenty-ninth feature set to 80 / 100 by adding smoke fixture execution planning with mutation-safe deck factory metadata, seeded account/storage-state review, targeted low-cost flow selection, disabled-by-default browser execution handoff checks, production-health and release-handoff serialization, env-driven fixture metadata, and regression coverage.
- Advanced the twenty-ninth feature set to 60 / 100 by adding production data operations for admin/auth/deck health, including a shared server-side data snapshot, seeded fixture guidance, stale share/session/revision cleanup runbooks, orphan-record review, asset storage hygiene, owner-visible admin dashboard remediation rows, production-health CLI serialization, and regression coverage.
- Advanced the twenty-ninth feature set to 40 / 100 by adding a scoped desktop OS recent-document writer for app-owned `.essdeck` files, including reusable writer planning, Local file status and release-registration metrics, a Tauri shell command that keeps legacy JSON open-compatible but unclaimed, and targeted TypeScript/Rust regression coverage.
- Started the twenty-ninth feature set at 20 / 100 by adding reusable native PPTX package authoring that composes ready theme, slide-master, slide-layout, comment-thread, action-setting, and animation-timing XML into existing package entries with merge-aware content type, relationship, action overlay, and timing handling; surfaced composed package metrics in PPTX preflight and covered the writer in presentation regression tests.
- Completed the twenty-eighth feature set at 100 / 100 by adding desktop/cloud production readiness health checks for scoped local save/open diagnostics, low-cost smoke fixtures, and admin/auth/deck data integrity; added a `bun run production:health` text/JSON CLI, extended release handoff commands, covered the new reports in regression tests, and defined the twenty-ninth feature set around deeper Office fidelity, desktop OS release polish, production data operations, smoke execution depth, and accessibility/keyboard parity.
- Advanced the twenty-eighth feature set to 80 / 100 by adding a release automation handoff with CLI-readable text/JSON artifacts, Vercel/Turso/Better Auth/Brevo/admin environment readiness checks, desktop release/smoke/cloud-sync readiness composition, a `bun run release:handoff` script that avoids builds and deployments by default, and regression coverage for the handoff contract.
- Advanced the twenty-eighth feature set to 60 / 100 by adding collaboration reviewer operations for owner-visible PowerPoint comment-thread export choices, role-aware merge previews, shared-deck audit snapshots, serialized operations reports, and review-panel copy choices for handoff, thread plan, XML authoring, and shared review exports.
- Advanced the twenty-eighth feature set to 40 / 100 by adding standalone Essence theme-file export/import payloads with paired theme bundles, Office theme metadata, package manifests, and authored theme XML readiness; added reusable template package manifests with stale, unused, unpaired, and mismatched-theme diagnostics; wired the flows into the existing theme/template panels; and covered the portability path in presentation regression tests.
- Started the twenty-eighth feature set at 20 / 100 by adding editable Matrix and Pyramid diagram templates, preserving diagram source-template group metadata through insertion/copy/duplicate flows, adding a reusable diagram authoring report, surfacing PowerPoint-friendly diagram conversion metrics in PPTX preflight snapshots, and covering the path in presentation regression tests.
- Completed the twenty-seventh feature set at 100 / 100 by expanding native PPTX animation timing XML to stable entrance, emphasis, exit, and motion-path targets, including scale, rotation, fly-out, and motion-path XML primitives, updated Office review classifications, advanced-animation preflight metrics, regression coverage, and the twenty-eighth feature set plan.
- Advanced the twenty-seventh feature set to 80 / 100 by adding native PPTX action-setting XML authoring for safe slide jumps and URL actions, including transparent action overlay shape XML, slide relationship XML, Office click-action attributes, preflight and snapshot metrics, serialization, and regression coverage.
- Advanced the twenty-seventh feature set to 60 / 100 by adding native PowerPoint comment XML authoring for imported threaded comments, including comment authors, slide comment parts, slide comment relationships, preserved thread/reply metadata, preflight and snapshot metrics, serialization, and regression coverage while keeping manual comments in notes.
- Advanced the twenty-seventh feature set to 40 / 100 by adding concrete slide-master and slide-layout placeholder XML authoring, including native date/footer/slide-number placeholders, saved layout placeholder EMU geometry, master/layout XML preflight metrics, snapshot grouping, serialization, and regression coverage.
- Started the twenty-seventh feature set at 20 / 100 by adding concrete reusable Office theme package XML authoring for content types, root relationships, `ppt/theme/theme1.xml`, and theme relationship manifests, surfacing authored XML readiness and size in PPTX preflight/snapshots, and covering package-path serialization in presentation regression tests.
- Completed the twenty-sixth feature set at 100 / 100 by adding shared desktop release-gate metadata, Local file panel release-registration diagnostics, optional smoke execution readiness for seeded credentials, mutation-safe fixtures, targeted browser-flow selection, copyable release-review serialization, and the twenty-seventh feature set plan.
- Advanced the twenty-sixth feature set to 80 / 100 by adding a reusable Office action/animation review plan with safer action catalog classifications, native/emphasis/exit/motion-path animation handoff classes, exact slide-object owner actions in PPTX preflight warnings, grouped snapshot metrics, and regression coverage for action blockers plus advanced animation metadata.
- Advanced the twenty-sixth feature set to 60 / 100 by adding typed Office/ODP interoperability repair planning for linked chart data, rotated chart editability fallbacks, ODP table preset mapping, and native table structure readiness, surfacing the plan in PPTX preflight metrics/snapshots, and covering the action-oriented serialization path in presentation regression tests.
- Advanced the twenty-sixth feature set to 40 / 100 by adding a native master/layout XML authoring task plan for theme package parts, slide-master placeholders, and saved slide-layout placeholders, surfacing authoring readiness and handoff counts in PPTX preflight/snapshots, and covering the new authoring geometry and serialization path in presentation regression tests.
- Started the twenty-sixth feature set at 20 / 100 by adding reusable `.thmx` theme package planning, stable package-part contracts for content types, root relationships, theme XML, and theme relationship manifests, serialized theme package review notes, PPTX preflight metrics for theme package/file readiness, and regression coverage tied into the native master XML plan.
- Completed the twenty-fifth feature set at 100 / 100 by broadening production smoke coverage across email/password auth, share-link download permissions, Backstage recovery/info tabs, desktop file handoff panels, and cloud conflict selectors while keeping browser execution optional; defined the twenty-sixth feature set around deeper Office fidelity, richer ODP/table/chart repair, desktop release hardening, and optional smoke-runner execution.
- Advanced the twenty-fifth feature set to 80 / 100 by adding reusable compatibility repair reporting for linked chart data, chart fallback editability, ODP table preset cleanup, and Office theme-file handoff readiness, threading ODP import context into Backstage PPTX preflight, and adding repair-focused snapshot/UI/regression coverage.
- Advanced the twenty-fifth feature set to 60 / 100 by adding reusable large-deck slide-window and virtual filmstrip contracts, status-bar window telemetry, production-readiness diagnostics for active slide windows and canvas detail budgets, and regression coverage for very large deck rendering safeguards.
- Advanced the twenty-fifth feature set to 40 / 100 by adding reusable PPTX action-setting metadata, slide-level action handoff notes in exported PPTX speaker notes, native action-setting preflight metrics, and safer animation XML support reporting for supported effects with unstable Office targets.
- Started the twenty-fifth feature set at 20 / 100 by adding app-owned `.essdeck` deck saves with legacy `.json` open compatibility, installable Tauri file-association metadata, scoped Rust deck read/write extension support, desktop release-registration preflight/reporting, and regression coverage for the new registration path.
- Completed the twenty-fourth feature set at 100 / 100 by adding stable browser-smoke selectors for status-bar pressure, Backstage export preflight, and desktop packaging status, broader low-cost smoke contracts, reusable production pressure diagnostics, joined production-readiness reporting, and the twenty-fifth feature set plan.
- Advanced the twenty-fourth feature set to 80 / 100 by adding typed desktop packaging readiness for file-association plans, OS recent-document registration metadata, signed-package checks, native open/save edge cases, desktop preflight reporting, and regression coverage.
- Advanced the twenty-fourth feature set to 60 / 100 by adding richer review-thread reply controls, explicit owner/editor/viewer merge-resolution choices, audit-ready collaboration operation summaries, and regression coverage for thread reply depth plus local/cloud conflict choices.
- Advanced the twenty-fourth feature set to 40 / 100 by adding native PPTX master XML package planning for theme, slide-master, and slide-layout parts, reusable theme-file handoff metadata, placeholder inheritance diagnostics, export-preflight metrics, snapshot grouping, and regression coverage.
- Started the twenty-fourth feature set at 20 / 100 by adding reusable PPTX comment-thread XML planning for imported PowerPoint root/reply groups, manual comment note handoffs, anchor-readiness metrics in export preflight, and targeted regression coverage.
- Completed the twenty-third feature set at 100 / 100 by adding disabled-control assertions to the cloud-sync browser smoke contract, adding a review-only merge flow, validating the expanded e2e readiness path, and defining the twenty-fourth feature set around deeper Office fidelity, review operations, desktop packaging, performance, and smoke coverage.
- Advanced the twenty-third feature set to 80 / 100 by adding role-aware review-thread lifecycle grouping for manual and imported PowerPoint threads, safer deck merge summaries with viewer merge blocking and conflict-area grouping, cloud merge review UI wiring, and regression coverage.
- Advanced the twenty-third feature set to 60 / 100 by adding native PPTX timing XML for simple fade-out exit animations, updating animation handoff reporting for native exit support, and adding PPTX export-preflight metrics for native exit animations plus PowerPoint comment-thread, reply, and manual-note handoff planning.
- Advanced the twenty-third feature set to 40 / 100 by adding presenter cleanup actions for rehearsal drift, missing rehearsal, speaker-note polish, missing media captions, audience-display setup, and screen-handoff blockers, with slideshow panel actions and regression coverage.
- Started the twenty-third feature set at 20 / 100 by deepening action-button authoring with selectable style variants, selected-object slide-jump diagnostics, copyable PPTX action-setting handoff reports, PPTX preflight action metrics, and regression coverage for grouped button dedupe, external links, and broken targets.
- Completed the twenty-second feature set at 100 / 100 by adding animation-density deck scale metrics, status-bar animation pressure visibility, regression coverage for animation-heavy slide overloads, and defining the twenty-third feature set around action-button authoring, presenter polish, native Office export fidelity, collaboration operations, and production hardening.
- Advanced the twenty-second feature set to 80 / 100 by adding richer object animation variants for fly-left/fly-right entrances, grow-shrink/teeter emphasis, fade-out-down exits, and motion-left paths, with shared validation options, preview/slideshow CSS classes, and regression coverage.
- Advanced the twenty-second feature set to 60 / 100 by adding owner-copyable collaboration handoff reports for unresolved review comments, including PowerPoint root-thread, reply-thread, speaker-note handoff classification, role-aware merge/share/export policy summaries, review-panel copy controls, and regression coverage.
- Advanced the twenty-second feature set to 40 / 100 by adding permission-scoped Rust/Tauri file APIs for dialog-backed native deck read/write, presentation and outline imports, image-slide imports, export writes, native current-file save reuse, native recent-path metadata, and desktop readiness detection, with Rust unit coverage and presentation regression coverage.

## 2026-05-15

- Started the twenty-second feature set at 20 / 100 by adding real multi-series chart editing with Category/Series TSV data, explicit chart-series persistence, multi-series canvas preview, native PPTX multi-series export, PPTX/ODP series preservation, server validation, and presentation regression coverage.
- Completed the twenty-first feature set at 100 / 100 by adding grouped PPTX preflight snapshot sections for native master/layout readiness, chart/table fidelity, and media packaging decisions, plus styled action-button payloads with preview swatches and target-slide regression coverage; defined the twenty-second feature set around deeper Office fidelity, permission-scoped Tauri file APIs, collaboration review depth, authoring controls, and production hardening.
- Advanced the twenty-first feature set to 80 / 100 by adding permission-scoped desktop file command payloads for open/save/import/export/recover actions, surfacing recent-file native handoff metadata in the local-file status dialog, and covering the payload and stale-recent behavior in the presentation regression suite.
- Advanced the twenty-first feature set to 60 / 100 by adding copyable shared-link review reports to the public deck review sidebar, preserving unresolved comments, mention labels, PPTX thread IDs, slide references, target labels, and download-permission state with regression coverage for report serialization.
- Advanced the twenty-first feature set to 40 / 100 by preserving ODP table-cell style metadata during editable table import, including cell background fill, text color, bold weight, and border color, with regression coverage for style-backed imported table cells.
- Started the twenty-first feature set at 20 / 100 by importing ODP multi-series local chart tables as flattened editable Essence chart data, adding chart preflight summaries for local-table, multi-series, and linked-data review counts, and extending regression coverage for multi-series chart labels and values.
- Completed the twentieth feature set at 100 / 100 by adding shared-view review snapshots for download permission, unresolved comments, mentions, PPTX thread handoffs, and note coverage in view-only deck surfaces, plus defining the twenty-first feature set around deeper ODP fidelity, collaboration review operations, desktop file permissions, export confidence, and authoring polish.
- Advanced the twentieth feature set to 90 / 100 by adding typed PPTX animation handoff support reporting that separates native XML entrance effects from speaker-note-only emphasis/exit/motion effects, surfaces native animation XML, handoff-only, and effect-kind metrics in export preflight, and enriches speaker-note cues with animation kind and support path.
- Advanced the twentieth feature set to 80 / 100 by importing common ODP chart objects from embedded package `content.xml` files as editable Essence charts, including chart title, chart class, local-table data, legend detection, frame geometry, chart preflight metrics, and regression coverage.
- Advanced the twentieth feature set to 70 / 100 by importing common ODP transition type, transition-speed, and page-duration metadata into Essence slide transitions and auto-advance timing, with preflight messaging and regression coverage.
- Advanced the twentieth feature set to 60 / 100 by importing supported ODP `draw:plugin` package audio/video as editable inline playback media, adding manifest/path MIME resolution, frame geometry preservation, media preflight counts, and regression coverage for playable imported media sources.
- Advanced the twentieth feature set to 40 / 100 by importing ODP `table:table` objects as editable Essence tables with row/column cell text, frame geometry, grid/header defaults, simple span metadata, preflight table counts, and regression coverage that keeps table cell text out of duplicated slide body text.
- Started the twentieth feature set at 20 / 100 by adding a native PPTX master/layout export readiness model for footer/date/slide-number defaults, reusable layout placeholder candidates, unsupported slot handoffs, Office theme font/color coverage, and Backstage PPTX preflight metrics with regression coverage.
- Completed the nineteenth feature set at 100 / 100 by adding deterministic PPTX preflight snapshots to Backstage Export with copy/download actions, stable report filenames, mixed-feature compatibility snapshot regression coverage, and a defined twentieth feature set for deeper native PPTX master/layout fidelity, ODP conversion, advanced animation, collaboration/review hardening, and Tauri desktop integration.
- Advanced the nineteenth feature set to 90 / 100 by adding richer media editing review and recording replacement workflows, including trim/caption alignment checks, presenter handoff summaries in the media panel, selected matching audio/video capture replacement with trim reset, and regression coverage for the review and replacement policies.
- Advanced the nineteenth feature set to 80 / 100 by preserving imported PPTX Office theme metadata, including theme names, color schemes, major/minor fonts, master/layout counts, and placeholder default counts through deck normalization, server validation, Master safeguard reporting, and native PPTX font export, with regression coverage for parser, report, and export behavior.
- Advanced the nineteenth feature set to 60 / 100 by adding a typed desktop/local media resolver handoff for PPTX export, allowing browser-local and file-reference media to be packaged as native PowerPoint media once resolved to inline data URLs, with preflight metrics, export-report counts, handoff-note alignment, and regression coverage for real package entries.
- Advanced the nineteenth feature set to 40 / 100 by importing basic ODP drawing shapes as editable Essence shapes, including rectangles, ellipses, lines, connectors, common custom-shape kinds, ODP frame geometry, fill/stroke color, stroke width, dash style, and regression coverage for shape/style preservation.
- Started the nineteenth feature set at 20 / 100 by importing ODP embedded package images as real deck assets and positioned editable image objects, updating ODP preflight scope messaging, and adding regression coverage for image asset/frame preservation.
- Completed the eighteenth feature set at 100 / 100 by importing supported embedded PPTX audio/video media as editable playback objects, avoiding duplicate media-cover image imports, making media compatibility warnings flag only unsupported formats, and defining the nineteenth feature set around deeper ODP conversion, desktop media handoffs, true master/theme metadata, advanced recording controls, and production fixtures.
- Advanced the eighteenth feature set to 80 / 100 by adding a real browser recording workflow for microphone audio, camera video, and screen capture, including a shadcn-style recorder dialog, capture readiness gates, MIME selection, preview/discard/insert controls, deterministic recorded media filenames, and regression coverage for recording helper behavior.
- Advanced the eighteenth feature set to 60 / 100 by exporting supported inline audio/video sources as native PPTX media, adding native-vs-fallback media export decisions, preflight metrics, media handoff notes, and package-level regression coverage for embedded media relationships.
- Advanced the eighteenth feature set to 40 / 100 by adding Office table style metadata retention, PPTX table cell fill/text/border-side import, selected-cell border-side controls, side-border rendering through canvas/SVG/PPTX, server validation persistence, export preflight metrics, and regression coverage.
- Started the eighteenth feature set at 20 / 100 by adding master/theme safeguard reporting for theme-file metadata gaps, master inheritance coverage, manual placeholder overrides, locked placeholders, duplicate/stale master presets, and bulk reapply risk slides, with a Master panel surface and regression coverage.
- Completed the seventeenth feature set at 100 / 100 by adding media handoff/source compatibility reporting, browser recording API readiness, PPTX preflight metrics for media source candidates, trim handoffs, and caption cues, plus regression coverage; defined the eighteenth feature set around master/theme safeguards, table round-trip fidelity, native media embedding, real recording capture, and deeper import conversion.
- Advanced the seventeenth feature set to 92 / 100 by adding timed media caption cues with VTT/SRT parsing, caption cue normalization, a reusable media caption control, generated browser caption tracks for audio/video playback, caption summaries in static export placeholders, PPTX speaker-note handoff metadata, presenter-readiness caption counting, and regression coverage.
- Advanced the seventeenth feature set to 84 / 100 by adding coordinate-based table cell range selection, selected-range TSV editing, selected-range merge/split controls, selected-range fill/border/text/weight overrides, grid/executive table style presets, render/export-ready cell style normalization, and regression coverage for range editing plus style clearing.
- Advanced the seventeenth feature set to 70 / 100 by making deck master layout presets editable in place, including preset label/description edits, bounded slot geometry edits, slot text edits, and regression coverage for master preset mutation/clamping.
- Advanced the seventeenth feature set to 60 / 100 by adding linked master layout variants for saved deck master presets, including deterministic Mirror and Stack geometry transforms in the master preset UI plus regression coverage for source-preset lineage and variant slot geometry.
- Advanced the seventeenth feature set to 48 / 100 by making locked placeholders reserve matching built-in and custom-layout slots during layout reapply, preventing duplicate replacements while preserving protected placeholder content and geometry, with regression coverage.
- Advanced the seventeenth feature set to 40 / 100 by adding pulse, spin, fade-out, and motion-right object animations, category-aware slideshow sequencing for emphasis, exit, and motion-path playback, preview cleanup for exit effects, validation/persistence support, and regression coverage while keeping native PPTX XML limited to reliable entrance effects.
- Started the seventeenth feature set at 20 / 100 by adding a real animation pane for ordered object effects, selected-object add/remove, timing edits, preview, reorder controls, and slide-level timing summaries, with shared object labels and regression coverage for visible effect ordering, summaries, and reorder behavior.
- Completed the sixteenth feature set at 100 / 100 by adding a credential-free cloud-sync e2e readiness gate, validating selector contracts, flow selection, URL, timeout, and storage-state inputs, wiring it into pull-request and main-branch CI without Playwright or live credentials, and defining the seventeenth feature set around advanced animation, master/layout, table, and media parity.
- Advanced the sixteenth feature set to 80 / 100 by adding a Rust-side Tauri native menu bridge for File, Import, Export, and Recover commands, dispatching existing web command IDs into the editor without broad file permissions, and covering the bridge with Rust unit tests plus desktop preflight.
- Advanced the sixteenth feature set to 60 / 100 by exporting routed elbow and curved connectors as editable custom Office geometry instead of rasterized artwork, preserving route points, dash style, and arrowheads in PPTX XML while keeping speaker-note handoff cues for live object-bound connector rebuilds.
- Advanced the sixteenth feature set to 40 / 100 by adding native PPTX animation timing XML for supported entrance effects, mapping generated PowerPoint shape IDs to Essence objects through link overlays, preserving speaker-note handoff cues for review, and extending regression coverage for stale timing replacement, target IDs, delays, and durations.
- Started the sixteenth feature set at 20 / 100 by adding a native PPTX transition XML exporter adapter for supported fade, push, zoom, duration, and auto-advance settings, preserving transition speaker-note fallback, replacing stale transition XML during export, and extending import/export regression coverage for timing-only transitions.
- Completed the fifteenth feature set at 100 / 100 by adding PPTX transition/timing handoff notes for slide rebuild, counting transition cues in export preflight, and defining the sixteenth feature set around native PPTX transition XML, animation timelines, connector edit-point metadata, Tauri menu/file APIs, and broader e2e coverage.
- Advanced the fifteenth feature set to 98 / 100 by adding PPTX connector route handoff notes that preserve routed connector start/control/end points, arrowheads, and dash style in speaker notes, surfacing connector-route counts in export preflight, and extending regression coverage for the handoff.
- Advanced the fifteenth feature set to 96 / 100 by exporting speech bubbles as native PowerPoint rounded callout shapes instead of SVG artwork and updating object-conversion reporting so only routed connectors remain flagged for shape edit-point handoff.
- Advanced the fifteenth feature set to 94 / 100 by adding normalized table merged-cell metadata, merge-header and split-all controls, merged-cell canvas/SVG rendering, server validation, PPTX import preservation, native PPTX table serialization with row/column spans for non-rotated tables, rotated-table visual fallback, and regression coverage for span rendering and validation.
- Advanced the fifteenth feature set to 82 / 100 by adding PPTX animation handoff notes that preserve animated object order, effect labels, durations, delays, and click-trigger instructions in speaker notes while keeping preflight/object-conversion warnings honest about missing native PowerPoint timelines.
- Advanced the fifteenth feature set to 66 / 100 by exporting non-rotated Essence charts as native editable Office charts in PPTX, preserving chart labels, values, colors, legend state, and value-label settings while keeping static artwork fallback and preflight warnings for rotated charts.
- Advanced the fifteenth feature set to 50 / 100 by adding direct canvas drag handles for line and connector start/control/end edit points, keeping routed geometry synchronized with collaboration mutations, and extending regression coverage for canvas point projection and clamped drag patches.
- Advanced the fifteenth feature set to 34 / 100 by adding explicit connector and line routing geometry, editable start/control/end point controls, normalized cloud/server validation, PPTX import defaults, SVG/PPTX/canvas rendering support, and regression coverage for routed connector paths and validation clamps.
- Started the fifteenth feature set at 18 / 100 by adding first editable ODP import for slide order, title/body text, and speaker notes, wiring `.odp` imports into the real deck replacement flow, keeping ODP compatibility reports for richer objects, and adding regression coverage for partial conversion scope and imported editable slides.
- Completed the fourteenth feature set at 100 / 100 by adding a Backstage object-conversion report for PowerPoint handoff editability, covering native-like objects, simplified artwork, placeholders, hidden/skipped objects, connectors, custom shapes, media, charts, icons, tables, groups, animations, image effects, and text layout flattening; defined the fifteenth feature set around editable ODP intake, richer object geometry, deeper PPTX metadata, table structure, and native desktop command depth.
- Advanced the fourteenth feature set to 94 / 100 by adding a typed desktop menu command contract, native/browser/fallback channel reporting in Local file status, event dispatch/listening helpers for future Tauri menus, editor command wiring, and regression coverage for command readiness.
- Advanced the fourteenth feature set to 82 / 100 by making the slideshow presenter-readiness review actionable with issue-slide jumps, longest-slide navigation, per-slide timing application, all-slide rehearsed-timing application, and regression coverage for action-ready timing metadata.
- Advanced the fourteenth feature set to 66 / 100 by expanding presenter readiness into a slide-level rehearsal and notes review, including total/average/longest timing summaries, short-note warnings, auto-advance mismatch detection, and regression coverage.
- Advanced the fourteenth feature set to 50 / 100 by adding presenter-readiness reporting in slideshow mode, covering rehearsal timing, speaker notes, media captions, audience display, fullscreen availability, blank-screen handoff checks, and regression coverage.
- Advanced the fourteenth feature set to 34 / 100 by adding section-aware export summaries, printable section overview pages, section markers in handouts, section-grouped outline exports, and regression coverage for section handoff metrics.
- Started the fourteenth feature set at 18 / 100 by adding reusable slide-section helpers, current-section rename controls, whole-section move up/down actions, section-collapse persistence cleanup, and regression coverage for section rename/move behavior.
- Completed the thirteenth feature set at 100 / 100 by adding ODP package preflight for real slide/image/media/note/chart/animation/transition inspection, surfacing the latest report in File and Backstage, and defining the fourteenth feature set around authoring polish, presenter readiness, desktop contracts, and richer conversion paths.
- Advanced the thirteenth feature set to 82 / 100 by adding a PPTX export preflight that reports lossy PowerPoint handoff risks for media, animations, timings, charts, image effects, groups, comments, hidden objects, lists, columns, and master flattening directly in Backstage Export.
- Advanced the thirteenth feature set to 66 / 100 by adding a Backstage document-readiness dashboard for deck size, assets, cloud permissions, local save state, recovery coverage, compatibility warnings, and export handoff status.
- Advanced the thirteenth feature set to 50 / 100 by adding backstage recent-cloud-deck shortcuts with search, pinning, forget controls, signed-in readiness, and a cloud-open handoff that keeps autosave/conflict tracking coherent.
- Advanced the thirteenth feature set to 34 / 100 by adding searchable and pinnable recent local decks, individual forget actions, stale-handle cleanup, readiness warnings, and regression coverage for recent-deck organization.
- Started the thirteenth feature set at 18 / 100 by adding a PowerPoint-style backstage file hub with Home/Open/Export/Recover/Info tabs wired to existing local file, import, export, recovery, and compatibility-report actions.
- Completed the twelfth feature set at 100 / 100 by preserving PPTX threaded-comment parent/reply hierarchy metadata, surfacing reply context in review surfaces, validating the new metadata server-side, and defining the thirteenth feature set around PowerPoint-class backstage/file workflows.
- Advanced the twelfth feature set to 94 / 100 by adding current-deck thumbnails to image-slide import report rows while keeping open-slide controls and stale-position handling.
- Advanced the twelfth feature set to 88 / 100 by preserving PPTX comment anchors and basic threaded-comment identifiers as structured comment metadata, showing imported PPTX metadata in review surfaces, and keeping server validation from dropping the imported metadata.
- Advanced the twelfth feature set to 74 / 100 by making image-slide import report entries actionable with per-slide open controls that select the matching inserted slide or explain when the slide position is no longer available.
- Advanced the twelfth feature set to 62 / 100 by surfacing the latest image-slide import report in the File menu with inserted slide range, inserted count, and a direct clear action.
- Advanced the twelfth feature set to 50 / 100 by adding persisted image-slide import reports with inserted slide ranges, copy/download/clear controls, and regression coverage for report serialization.
- Advanced the twelfth feature set to 34 / 100 by adding image-slide orientation detection, dimensions, orientation summaries, duplicate-name warnings, and regression coverage for orientation and duplicate-name review behavior.
- Started the twelfth feature set at 18 / 100 by adding a staged image-slide import review dialog with preview thumbnails, slide count/size/type summaries, reorder/remove controls, and regression coverage for staged image ordering and removal.
- Completed the eleventh feature set at 100 / 100 by adding rollback checkpoint capture before recovery restores, keeping the selected snapshot available after restore, surfacing restore result messaging, and covering restore-checkpoint behavior in the regression harness.
- Advanced the eleventh feature set to 90 / 100 by importing basic PPTX comment text, authors, timestamps, and mentions into the existing slide comment review model while reporting unsupported threaded or position metadata as simplified.
- Advanced the eleventh feature set to 76 / 100 by importing basic PPTX slide transition metadata, including common transition type, duration, and auto-advance timing, while keeping unsupported transition details in compatibility warnings.
- Advanced the eleventh feature set to 60 / 100 by adding slide-level recovery diffs that identify added, removed, and changed slides with object-count movement before restoring a snapshot.
- Advanced the eleventh feature set to 42 / 100 by adding recovery snapshot summary copy, portable JSON snapshot download, safe recovery filenames, and regression coverage for recovery review serialization.
- Started the eleventh feature set at 24 / 100 by adding a recovery review dialog that compares local snapshots against the current deck before restore, with delete/clear actions and regression coverage for current, unchanged, and older-deck recovery cases.
- Completed the tenth feature set at 100 / 100 by adding a local-file readiness layer with toolbar status, capability details for open/save/recent/clipboard/recovery/desktop shell support, and regression coverage for fallback, browser-local, and desktop-shell modes.
- Advanced the tenth feature set to 86 / 100 by adding copy and text-download controls for active PPTX compatibility reports, backed by reusable report serialization and safe filename regression coverage.
- Advanced the tenth feature set to 72 / 100 by adding a dedicated PPTX compatibility review dialog with full warning details, recent-import selection, active-report clearing, and history cleanup outside the File menu.
- Advanced the tenth feature set to 48 / 100 by expanding persisted PPTX compatibility reports into a recent-import archive with active report selection, compatibility history clearing, and regression coverage for archive ordering and migration.
- Started the tenth feature set at 24 / 100 by persisting the latest PPTX compatibility report across reloads, clearing stale reports after clean imports, and covering report parsing in the regression harness.
- Completed the ninth feature set at 100 / 100 by separating current-deck recovery snapshots from older deck snapshots, adding scoped cleanup controls, and covering deck-scoped snapshot pruning in the regression harness.
- Advanced the ninth feature set to 82 / 100 by keeping the latest PPTX compatibility warnings in the File menu with clear controls, so skipped PowerPoint-only features remain reviewable after import.
- Advanced the ninth feature set to 66 / 100 by adding local recovery snapshots to the File menu with automatic deduped autosaves, restore controls, clear controls, and regression coverage for unchanged-state dedupe.
- Advanced the ninth feature set to 50 / 100 by adding PPTX compatibility warning detection for transitions, animations, media, embedded objects, ActiveX controls, SmartArt, comments, and macros, with import-time alerts and deterministic regression coverage.
- Advanced the ninth feature set to 34 / 100 by adding typed local deck file sessions, clean/dirty save status, read-only fallback import guidance, and regression coverage for timestamp-insensitive file status.
- Started the ninth feature set at 18 / 100 by adding portable custom master-style presets for house footer systems, including save, import, export, apply, delete, and regression coverage for JSON round trips.
- Started the eighth feature set at 20 / 100 by adding reusable slide-master style presets for common footer/date/number treatments, wiring them into the Master panel, and covering preset application/matching in the regression harness.
- Advanced the eighth feature set to 38 / 100 by adding a reusable asset audit for stale, unused, or unpaired custom templates and master layout presets, with compact template-dialog surfacing and regression coverage.
- Advanced the eighth feature set to 56 / 100 by making reusable asset audit issues actionable with direct cleanup controls for custom templates and deck-master layout presets.
- Advanced the eighth feature set to 74 / 100 by adding deterministic footer/date/number previews to slide-master style presets and regression coverage for preview placement.
- Advanced the eighth feature set to 88 / 100 by recommending recently saved custom brand kits and custom design palettes in their reuse dialogs, with regression coverage for recommendation ordering.
- Completed the eighth feature set at 100 / 100 by adding usage metadata and a maintenance panel for saved custom theme bundles, including stale, unused, and duplicate detection plus focused cleanup actions and regression coverage.
- Started the seventh feature set at 18 / 100 by adding safe collaboration mutation reconciliation, including history-aware accepted remote object patches, locked/missing/type-conflict skips, text update support, canvas apply controls, and regression coverage.
- Advanced the seventh feature set to 34 / 100 by adding deck-master layout preset usage metadata, recent-use sorting, last-used labels, and regression coverage for usage timestamps.
- Advanced the seventh feature set to 50 / 100 by adding custom template usage/freshness metadata, a template health panel, imported/stale/paired-theme stats, and regression coverage for usage tracking.
- Advanced the seventh feature set to 64 / 100 by adding remote mutation apply summaries so skipped locked, missing, or type-conflicting objects are visible after accepting collaborator updates.
- Advanced the seventh feature set to 78 / 100 by recommending recent deck-master layout presets on blank slides, preserving current/selected/delete actions, and covering recommendation ordering in the regression harness.
- Advanced the seventh feature set to 90 / 100 by recommending fresh custom templates with paired theme bundles and usage history, while keeping save/import/export and delete actions in the same reusable template cards.
- Completed the seventh feature set at 100 / 100 by turning skipped remote mutation summaries into actionable conflict details with local-object jump targets and regression coverage.
- Started the sixth feature set at 24 / 100 by adding portable custom theme bundles that capture deck palette, font pair, and full master defaults, apply built-in or saved bundles to the deck, and round-trip through JSON import/export with regression coverage.
- Advanced the sixth feature set to 42 / 100 by adding named deck-master layout presets that save tagged placeholder geometry into the deck master, apply to current or selected slides, survive validation/normalization, and can be removed from the master.
- Advanced the sixth feature set to 58 / 100 by adding permission-checked object-mutation collaboration events, throttled move/resize/rotate/text mutation publishing, live remote mutation ghost overlays, activity summaries, and regression coverage for mutation payload projection.
- Advanced the sixth feature set to 74 / 100 by pairing saved custom deck templates with captured theme bundles so reused templates preserve their visual system, show paired-theme swatches, and are covered by regression tests.
- Advanced the sixth feature set to 90 / 100 by adding master layout preset preview thumbnails plus portable JSON import/export with duplicate skipping and regression coverage.
- Completed the sixth feature set at 100 / 100 by adding portable custom deck-template JSON import/export, including paired theme-bundle round-trip coverage.
- Completed the fourth feature set at 100 / 100 and started the fifth feature set around reusable theme systems, brand portability, layout/master controls, and authoring polish.
- Added local custom design palettes that can be derived from the current deck, saved, reapplied to the current slide or whole deck, and deleted from the palette gallery, with regression coverage for palette derivation.
- Added reusable font-pair presets that update title/body typography on the current slide or whole deck, write deck master font defaults, persist through deck validation, and render through canvas, slideshow, SVG, and PPTX export paths.
- Added portable JSON import/export for saved custom design palettes, including a versioned palette file format, duplicate/cap handling, and regression coverage for round-trip parsing.
- Added editable placeholder roles for layout-applied objects so title, body, media, and caption placeholders persist, can be managed from the properties panel, survive validation, and are preferred when layouts are reapplied.
- Added reusable brand kits that bundle design palettes, font pairs, combined previews, and deck master typography defaults into one apply action with regression coverage for slide-scoped and deck-scoped behavior.
- Added local custom slide layouts that capture tagged placeholder geometry from the current slide, reapply saved title/body/media/caption slots while preserving existing content, and round-trip through versioned JSON parsing in regression coverage.
- Added custom brand-kit save/import/export so current deck visual systems can be captured as portable JSON bundles, restored later, and validated through regression coverage.
- Added selectable theme preview thumbnails to built-in deck templates so users can inspect and apply a palette variant before creating the full template deck, with regression coverage for themed template creation.
- Added selected-slide custom layout application so saved placeholder layouts can be applied to the current slide or an entire selected slide set through one history-safe operation, with regression coverage for selected and unselected slide behavior.
- Added custom slide-layout JSON import/export controls so saved placeholder systems can move between browsers and installs, with regression coverage for portable layout bundle import.
- Completed the fifth feature set at 100 / 100 by upgrading template theme variants into small theme bundles that apply palette, font pair, and master font defaults together.
- Started the fourth feature set by adding a shared deck-operation access policy for owner, editor, viewer, authenticated non-collaborator, and anonymous roles; wired deck presence access through it and covered the collaboration permission matrix in the regression harness.
- Added database-backed private deck collaborators with owner-managed editor/viewer APIs, collaborator payload validation, a Drizzle migration, cloud deck role metadata, role-aware deck deletion controls, and share-dialog collaborator management.
- Routed authenticated deck read, asset content, version read, save, merge, restore, share, delete, and presence paths through the shared deck-operation access policy so collaborator roles now affect real server behavior.
- Added role-aware cloud sync chrome so viewer collaborators cannot autosave, save, overwrite, restore, merge, share, or delete decks from the editor UI while still being able to open and compare allowed decks.
- Added durable notifications for collaborator add, role update, and removal events so both owners and affected collaborators get an inbox record of permission changes.
- Added owner/editor/viewer role badges to active deck presence so live collaborators show their current access level.
- Added pending private collaborator invitations for emails without accounts, visible pending states in the Share dialog, owner notification records, and automatic invite acceptance into active editor/viewer access when the invited email signs in and loads cloud decks.
- Added owner/editor/viewer role context to version history plus cloud conflict and merge review surfaces so restore and merge availability is visible beside the comparison UI.
- Added a permission-checked collaboration event ledger with cursor, selection, and edit-intent operation envelopes, client event de-duplication, API routes, client helpers, a Drizzle migration, and regression coverage for envelope validation.
- Wired the presence indicator to publish slide-selection collaboration events and render recent collaborator activity from the event stream with owner/editor/viewer role badges.
- Added canvas-level collaboration cursors with throttled cursor event publishing, event-stream polling, latest-visible remote cursor projection, role-colored markers, and regression coverage.
- Added remote selection and edit-intent overlays on the slide canvas, including selected-object event payloads, editing action publishing for move/resize/rotate/text work, and projection regression coverage.
- Added collaboration activity labels so the presence menu summarizes selected objects and edit-intent actions while filtering noisy cursor pings.
- Added `ASSET_STORAGE_DELETE_STALE_OBJECTS` configuration so remote object-store cleanup can either delete stale deck asset keys immediately or retain them for an external bucket lifecycle policy.
- Added deck scale metrics and status-bar large/heavy deck warnings with regression stress coverage for high-slide and high-object editor workloads.
- Added dense-slide canvas render budgeting with browser render containment and reduced unselected text-overflow decoration work while preserving checks on selected text objects.
- Started the third feature set by extracting presentation-store persistence snapshot and hydration merge normalization into a pure helper, filtering stale slide/element IDs, clearing transient preview/history state, clamping restored zoom, and covering the behavior in the regression harness.
- Added pure three-way deck merge groundwork for cloud collaboration, combining non-conflicting local/cloud slide and asset edits while reporting true same-slide and delete-versus-edit conflicts in the regression harness.
- Wired clean three-way merge decisions into the cloud conflict panel by tracking the last synced base deck, computing merge candidates against the current cloud copy, and saving merged decks only when the helper reports no true conflicts.
- Split the cloud merge review into a dedicated component that shows clean merge counts or concrete conflict reasons beside the existing cloud conflict preview.
- Added a server-side deck merge endpoint and repository helper so clean cloud conflict merges are validated against the current owner deck before persistence, with client merge actions now using that endpoint.
- Split presentation regression helpers into focused modules and expanded deck merge coverage for identical dual edits, intentional local deletions, and duplicate created asset-id conflicts.
- Added stable cloud-sync browser selectors across the real deck browser, conflict preview, merge review, compact conflict banner, and cloud deck row actions so automated conflict-path e2e tests can target functional controls.
- Added a typed cloud-sync browser e2e contract covering deck open, clean merge, manual conflict inspection, and open-cloud/overwrite escape flows, with regression validation that selectors are unique and referenced.
- Added a dependency-light cloud-sync e2e runner plus DOM driver for executing the browser flow contract, and covered runner ordering in the presentation regression harness.
- Added a locator-based browser driver and optional Playwright smoke script for cloud-sync e2e flows, with regression coverage for selector-to-locator execution and a package script for prepared browser environments.
- Added a manual GitHub Actions cloud-sync e2e smoke workflow that installs Playwright on demand and runs the browser smoke script against a supplied app URL and optional authenticated storage state.

## 2026-05-14

- Chose the hosted-web Tauri packaging mode for the desktop app and added a typed desktop preflight script that validates the production URL, dev URL, release build command, window sizing, permissions, and release gates before packaging work.
- Added desktop-style file picker support for opening Essence JSON decks and saving JSON, PPTX, PDF, SVG, and PNG files, with automatic download fallback where picker APIs are unavailable.
- Added desktop-style Ctrl/Command+O and Ctrl/Command+S shortcuts for opening deck files and saving the current deck JSON.
- Added same-file JSON save handling so Ctrl/Command+S writes back to the picker-selected deck file when the platform supports writable file handles.
- Added a desktop File menu with open/save/import/export actions plus IndexedDB-backed recent local JSON deck handles for quick reopen on supported browser engines.
- Extended picker-backed desktop import dialogs to PPTX files, text/Markdown outlines, and multi-image slide imports with fallback to the existing browser inputs.
- Added clear-recents management for local deck file handles stored by the desktop File menu.
- Added a runnable Bun presentation regression harness for default deck creation, outline import, safe links, rich text toggles, comment mentions, image asset migration/compaction, and undo-history trimming.
- Expanded the presentation regression harness with cloud deck organization, conflict preview, and server deck-payload validation coverage.
- Split admin access decisions into a pure policy helper and covered unauthenticated, forbidden, banned-admin, and active-admin outcomes in the regression harness.
- Split admin user-update validation into a pure policy helper and covered missing-target, self-demotion, self-ban, role, verification, and ban patch outcomes in the regression harness.
- Split deck share-link update validation into a pure policy helper and covered expiry, boolean, access-code generate, and access-code clear outcomes in the regression harness.
- Split deck-presence heartbeat normalization and display initials into a pure policy helper and covered blank slide IDs, trimmed slide IDs, name initials, and email fallback initials in the regression harness.
- Split notification mention matching, comment preview, and share-view dedupe timing into pure policy helpers and covered mention key variants, whitespace-compacted previews, long-preview caps, and the 15-minute dedupe cutoff in the regression harness.
- Expanded the presentation regression harness with editor selection geometry coverage for nudge clamping, marquee filtering, alignment, distribution, resize patches, and rotation normalization.
- Expanded the presentation regression harness with export filename, SVG escaping, and printable handout serialization coverage.
- Hardened server environment normalization so Better Auth, Turso, and Vercel URL settings ignore hidden BOM prefixes and blank values before auth initialization.
- Added large-deck performance controls: filmstrip thumbnail windowing, undo/future history memory budgeting, and status-bar deck/history size telemetry.
- Added persisted slide section breaks with filmstrip labels, slide-format editing, cloud validation, cloud save/load support, and a Drizzle migration.
- Added collapsible filmstrip sections with an expand-all control and selection-safe collapse behavior.
- Added a presenter-side slide navigator with section labels, active-slide state, and direct jump controls during slideshow playback.
- Added basic table objects with toolbar insertion, row/column/cell editing, header row styling, canvas/slideshow rendering, accessibility checks, cloud validation, SVG/PNG/PDF support, and PPTX export.
- Added basic chart objects with toolbar insertion, editable bar/line data, labels, legend/value toggles, canvas/slideshow rendering, accessibility checks, cloud validation, SVG/PNG/PDF support, and PPTX-rendered export.
- Added a basic shape library with rectangle, rounded rectangle, ellipse, diamond, line, and arrow variants plus fill/stroke controls, validation, SVG/PNG/PDF support, and PPTX export.
- Added real slide layout presets with non-destructive placeholder application for title, title/body, section, two-content, comparison, quote, and picture-caption layouts.
- Added deck-level slide master footer, date, and slide-number settings with editor/slideshow rendering plus SVG, PNG, PDF, handout, and PPTX export support.
- Added per-object slideshow entrance animations for fade, rise, zoom, and wipe with duration and delay controls.
- Added an editable deck template gallery with startup pitch, project update, and lesson plan templates.
- Added a deck proofing scanner for common typos, repeated spacing, and repeated punctuation with jump-to-text navigation.
- Added local custom templates that save the current deck and can be reused from the template gallery.
- Added local video objects with insertion, replacement, canvas/slideshow playback, validation, accessibility checks, and export placeholders.
- Added local audio objects with insertion, replacement, canvas/slideshow playback, validation, accessibility checks, and export placeholders.
- Added a symbol and equation-snippet gallery that inserts editable slide text objects.
- Added editor-side preview playback for selected object entrance animations.
- Added object action links with safe URL handling, slideshow click-through, accessibility warnings, and SVG/PPTX export support.
- Added internal slide-jump actions for linked objects in slideshow and PPTX exports.
- Added a grouped action-button gallery for first, previous, next, and last slide navigation.
- Added an editable grouped diagram gallery for process, cycle, and hierarchy SmartArt-style layouts.
- Added a relationship diagram template with editable overlapping concept areas.
- Added built-in editable icon objects with an icon gallery, styling controls, SVG export, and PPTX SVG embedding.
- Added grouped editable sticker templates for common slide callouts.
- Added a design palette gallery that can recolor the current slide or the whole deck through existing slide and object styles.
- Added selected-object duplication from the toolbar and Ctrl+D, including grouped objects.
- Added a persisted slide grid toggle for canvas alignment work.
- Added a persisted ruler overlay and canvas undo/redo keyboard shortcuts.
- Added canvas Ctrl+A selection and Escape selection clearing shortcuts.
- Added in-app object copy/paste from the toolbar and Ctrl+C/Ctrl+V, including grouped objects.
- Added true front/back layer ordering for selected and grouped objects.
- Added undoable object cut from the toolbar and Ctrl+X, including grouped objects.
- Added Ctrl+Bracket layer ordering shortcuts for step forward/back and Shift-assisted front/back moves.
- Added canvas Ctrl+G and Ctrl+Shift+G shortcuts for grouping and ungrouping selected objects.
- Added canvas slide shortcuts for Ctrl+M creation, selection-free Ctrl+Shift+D duplication, and PageUp/PageDown/Home/End navigation.
- Added a format-painter style clipboard for copying and applying object text, fill, stroke, table, chart, and image formatting.
- Added Tab and Shift+Tab canvas traversal for cycling through editable objects without the mouse.
- Added canvas zoom shortcuts for Ctrl+=, Ctrl+-, and Ctrl+0 reset.
- Added a status-bar zoom slider with an explicit reset control.
- Added layer-pane step ordering controls for moving objects forward and backward without leaving the selection pane.
- Added additive layer-pane selection with Ctrl/Command-click and Shift-click.
- Added external canvas paste support for clipboard images and plain text when no in-app objects are copied.
- Added slide-canvas drag-and-drop insertion for image files, text files, and dropped plain text.
- Positioned dropped image and text objects at the drop point instead of the generic insert location.
- Added whole-slide copy, cut, and paste from the toolbar and canvas shortcuts when no slide objects are selected.
- Added drag-and-drop filmstrip slide reordering with a typed insertion action in the presentation store.
- Changed new-slide insertion to place slides after the current slide instead of always appending to the end.
- Added canvas Ctrl+Shift+C/Ctrl+Shift+V object format-copy shortcuts and selection-free Alt+Arrow slide reordering.
- Added persistent filmstrip multi-slide selection and extended slide copy, cut, paste, and delete to selected slide sets.
- Added Shift-click range selection in the filmstrip while keeping Ctrl/Command-click additive selection.
- Added status-bar telemetry for multi-slide and object selection counts.
- Upgraded slide reordering so selected slide sets move together from toolbar buttons, Alt+Arrow shortcuts, and filmstrip drag-and-drop.
- Upgraded slide duplication so selected slide sets duplicate together and remain selected after insertion.
- Added filmstrip keyboard workflows for select all, copy, cut, paste, duplicate, delete, and Alt+Arrow slide-set reordering.
- Added filmstrip Arrow/Home/End navigation with Shift-assisted range selection.
- Added a compact filmstrip slide action menu for selected-slide copy, cut, paste, duplicate, and delete workflows.
- Added current-section selection from the filmstrip action menu so section-wide copy, cut, duplicate, delete, and reorder workflows use the existing slide-set engine.
- Added filmstrip section actions for starting a section at the active slide and clearing section breaks from selected slides.
- Added collapse-all section control with selection-safe fallback to the containing section header.
- Expanded editable shapes with triangle, pentagon, hexagon, parallelogram, and trapezoid variants across editor rendering, import normalization, SVG/PNG/PDF export, and PPTX export.
- Added a toolbar shape gallery that inserts the chosen editable shape directly with sensible canvas sizing.
- Added right arrow, chevron, plus, and star shape variants to the editable shape catalog and PPTX export mapping.
- Added solid, dashed, dotted, and dash-dot stroke styles for shapes and lines across editor rendering, cloud validation, SVG/PNG/PDF export, style copy/paste, and PPTX export.
- Upgraded filmstrip thumbnails so editable shapes render with their real silhouette and stroke style instead of a generic rectangle.
- Added start and end arrowhead controls for line shapes with triangle, diamond, and oval endpoints across editor rendering, cloud validation, SVG/PNG/PDF export, style copy/paste, and PPTX export.
- Added a double-arrow line preset to the shape gallery with matching default endpoints and palette behavior.
- Added table style presets for plain, accent, dark, and minimal tables across canvas/slideshow rendering, cloud validation, SVG/PNG/PDF export, style copy/paste, and PPTX export.
- Added one-click table row and column add/remove controls that preserve existing cell content while resizing.
- Added a banded-rows table option across canvas/slideshow rendering, cloud validation, SVG/PNG/PDF export, style copy/paste, and PPTX export.
- Added banded columns plus first-column and last-column table emphasis options across canvas/slideshow rendering, cloud validation, SVG/PNG/PDF export, style copy/paste, and PPTX export.
- Added a total-row table option across canvas/slideshow rendering, cloud validation, SVG/PNG/PDF export, style copy/paste, and PPTX export.
- Added visual transition preset thumbnails in the slide properties panel for none, fade, push, and zoom transitions.
- Added a slideshow progress scrubber for direct slide jumping from the bottom control bar.
- Added presenter black-screen and white-screen blanking controls with keyboard toggles during slideshow playback.
- Added image opacity, brightness, contrast, and saturation adjustment controls across editor/slideshow rendering, cloud validation, SVG/PNG/PDF export, and style copy/paste.
- Added start/end trim controls for local video and audio objects with playback enforcement, cloud validation, SVG/PDF placeholders, and PPTX placeholder labels.
- Added editable captions for local video and audio objects across editor/slideshow rendering, cloud validation, SVG/PDF placeholders, and PPTX placeholder labels.
- Added a slideshow autoplay option for local video and audio objects with editor-safe playback scoping, cloud validation, SVG/PDF placeholders, and PPTX placeholder labels.
- Added pie chart support across chart editing, canvas/slideshow rendering, cloud validation, SVG/PNG/PDF export, and PPTX-rendered export.
- Added donut chart support across chart editing, canvas/slideshow rendering, cloud validation, SVG/PNG/PDF export, and PPTX-rendered export.
- Added area chart support across chart editing, canvas/slideshow rendering, cloud validation, SVG/PNG/PDF export, and PPTX-rendered export.
- Added horizontal bar chart support across chart editing, canvas/slideshow rendering, cloud validation, SVG/PNG/PDF export, and PPTX-rendered export.
- Added editable elbow connector shapes with gallery insertion, stroke/dash/arrowhead controls, canvas/slideshow rendering, cloud validation, SVG/PNG/PDF export, and PPTX export.
- Added editable curved connector shapes with gallery insertion, stroke/dash/arrowhead controls, canvas/slideshow rendering, cloud validation, SVG/PNG/PDF export, and PPTX export.
- Added editable speech bubble callout shapes with gallery insertion, fill/stroke/dash controls, canvas/slideshow rendering, cloud validation, SVG/PNG/PDF export, and PPTX export.
- Added table text alignment controls across canvas/slideshow rendering, SVG/PNG/PDF export, style copy/paste, and PPTX export.
- Added table vertical alignment controls across canvas/slideshow rendering, SVG/PNG/PDF export, style copy/paste, cloud validation, and PPTX export.
- Added a dashboard console with account metrics, user management, an embedded website/editor view, Brevo email confirmation codes, admin-role schema fields, and a seeded-admin script.
- Removed public-facing development-stack copy from the app metadata, README overview, and roadmap intro.
- Added admin deck and session management tables with protected APIs for reviewing saved decks, deleting deck records, inspecting sessions, and revoking stale sessions.
- Added owner-managed read-only deck share links, protected share-link APIs, a public shared-deck viewer, and a Drizzle migration for deck sharing.
- Upgraded the public shared-deck viewer with notes, outline navigation, open-comment review, and viewer-side PDF/PPTX downloads.
- Added share-link activity tracking with persisted view events, owner-visible view counts, and last-viewed timestamps in the share dialog.
- Added optional share-link expiry controls with server-side expired-link enforcement and a focused share-row component split.
- Added optional access-code protection for shared decks with hashed codes, owner code controls, and a gated public viewer.
- Added owner-controlled download permissions for shared decks so public viewers can be limited to in-browser viewing.
- Added mention-aware review comments with stored mention metadata, editor/shared review badges, handout output, and PPTX notes output.
- Added an editor deck-wide review queue with previous/next navigation across open comments and target-object selection.
- Added saved-version comparison from version history by loading revision snapshots and reusing the deck difference preview.
- Added manifest-style cloud delivery for large saved image assets with owner-only content streaming routes and save-time asset rehydration.
- Added a synced second-window audience display for presenter mode so the public slide view stays separate from notes, timers, and private controls.
- Added first-pass PPTX import that extracts slide order, editable text boxes, speaker notes, basic backgrounds, and embedded images from PowerPoint files.
- Expanded PPTX import compatibility with theme color resolution and common Office preset shape/connector mapping into editable shape objects.
- Expanded PPTX import compatibility with PowerPoint tables and cached chart data mapped into editable Essence table and chart objects.
- Preserved imported PPTX object rotation for text, shapes, images, tables, and charts.
- Preserved imported PPTX grouped-object membership and mapped grouped child coordinates through their group transforms.
- Added PPTX slide-layout inheritance so imported slides keep closer layout labels, placeholder geometry/font defaults, and slide-layout/master background fallbacks.
- Added database-backed notifications with authenticated inbox APIs, a signed-in notification menu, and shared-deck view notifications for deck owners.
- Expanded owner notifications to cover shared-link creation, setting changes, deletion, and version-restore events.
- Added mention-targeted notifications for verified users referenced by unresolved review comments.
- Added authenticated deck presence with a heartbeat table, presence APIs, client heartbeat helpers, and a compact active-editor toolbar indicator.
- Added optional S3/R2-compatible object-store offload for large deck image assets while keeping authenticated asset content routes as the stable delivery path.
- Added a slideshow media-caption toggle that syncs subtitle visibility to the second-window audience display.
- Added click-through slideshow sequence triggers for revealing entrance-animated objects before advancing slides.

## 2026-05-13

- Created the Next.js 16, Bun, Tailwind CSS, shadcn/ui project scaffold in `G:\Powerpoint`.
- Initialized git for the workspace.
- Added Tauri v2 desktop shell configuration.
- Added Better Auth email/password server route and client helper.
- Added Drizzle + libSQL/Turso-ready schema for auth users, sessions, accounts, verification, decks, and slides.
- Added environment template and database migration scripts.
- Replaced the starter page with the first functional presentation editor.
- Added local deck persistence, slide CRUD, reorder, basic undo/redo, zoom, JSON import/export, speaker notes, basic object insertion, and object property editing.
- Added `todo.md` with the PowerPoint feature gap and project roadmap.
- Created the Turso Cloud database `essence-powerpoint`, generated the initial Drizzle migration, and applied it successfully.
- Added authenticated Turso deck APIs for listing, saving, loading, and deleting user-owned decks.
- Added a toolbar cloud sync panel for signed-in users to save and open decks from the database.
- Added direct canvas object dragging, bottom-right resizing, arrow-key nudging, delete-key removal, and z-order controls.
- Added multi-select state, multi-object delete/nudge, alignment controls, distribution controls, and per-object rotation.
- Added group dragging for multi-selected objects, empty-canvas marquee selection, and slide edge/center snap guides.
- Added an in-app slideshow mode with current-slide start, keyboard navigation, previous/next controls, and speaker notes.
- Added slideshow presenter tools: elapsed timer, per-slide rehearsal timing, laser pointer, ink annotations, and clear-ink/reset controls.
- Added per-slide transition settings for none, fade, push, and zoom with editable duration and slideshow playback animation.
- Added persisted rehearsal timings and per-slide auto-advance settings for slideshow playback.
- Added browser fullscreen request, fullscreen toggle, and fullscreen exit handling for slideshow mode.
- Added an editor-side transition preview action that reuses the slideshow animation effects on the canvas.
- Added a presenter-side next-slide preview panel with a compact read-only slide renderer.
- Added real image insertion from local files with deck-stored data URLs, shared image rendering, alt text, and contain/cover/fill controls.
- Added selected-image replacement plus crop X/Y controls for contain and cover image fit modes.
- Added multi-image import that creates one full-slide image slide per selected file.
- Added selected-slide SVG export with text, shape, image, background, and rotation serialization.
- Added browser-side raster image compression before storing imported images in deck data.
- Added self-contained printable handout HTML export with slide SVG previews and speaker notes.
- Added text and Markdown outline import that turns blank-line-separated blocks into new title/body slides.
- Added selected-slide PNG export through browser-side rasterization of the slide SVG serializer.
- Added real layer controls for selecting, hiding, showing, locking, and unlocking objects, with locked-object protection across canvas movement, resizing, deletion, layer movement, and slide exports.
- Added persistent object grouping and ungrouping with group-aware canvas selection, dragging, deletion, layer movement, local persistence, JSON export, and cloud deck validation.
- Added object-to-object snapping while dragging, including edge and center alignment against visible neighboring objects with live guide lines.
- Added a canvas rotation handle for single selected objects, including live drag rotation and Shift snapping to 15-degree increments.
- Added signed-in cloud autosave with debounce, last-known cloud version tracking, and server-side conflict detection to avoid silently overwriting newer remote deck copies.
- Added local recent cloud deck tracking with quick reopen and remove-from-recents controls in the cloud deck dialog.
- Added cloud conflict resolution actions to open the newer cloud copy or intentionally overwrite it with the current local deck.
- Added a multi-object transform box with proportional bottom-right resizing for selected editable objects.
- Added shared rotation for multi-selected and grouped objects from the canvas transform box.
- Added eight-handle resizing for multi-selected and grouped objects.
- Added eight-handle resizing for single selected objects plus a compact rotation readout.
- Added cloud conflict previews that compare the local and cloud deck before open or overwrite actions.
- Added a searchable, sortable cloud deck browser with locally pinned cloud decks.
- Added typed text alignment, line-height, bullet-list, and numbered-list formatting across canvas, slideshow, previews, export, and cloud validation.
- Added text columns and shrink-to-fit overflow behavior across text boxes, slide rendering, SVG export, and cloud validation.
- Added clipped-text indicators on selected text boxes with a properties-panel shortcut to shrink overflowing text.
- Added persisted image masks for rectangle, rounded, circle, and diamond image rendering across editor, slideshow, previews, SVG/PNG export, and cloud validation.
- Added selection-based rich text ranges for bold, italic, underline, and text color across text boxes, slide rendering, SVG/PNG export, and cloud validation.
- Added direct on-canvas text editing for title and text objects with double-click/Enter entry, Escape cancel, live content updates, and undo checkpointing.
- Added toggle-aware rich text controls and range splitting so partial clears preserve formatting outside the selected text.
- Added a deck-level image asset library with asset-id based image elements, deduped imports, local deck migration, slide SVG/PNG/handout export support, and Turso-backed `deck_asset` persistence.
- Added slide and object review comments with open/resolved state, selected-object attachment, canvas/status badges, print handout output, cloud validation, conflict detection, and a Drizzle migration for persisted slide comments.
- Added a deck accessibility checker for missing alt text, empty text boxes, clipped text, low contrast, tiny text, missing titles, status-bar issue counts, and jump-to-issue navigation from the right sidebar.
- Added native PowerPoint `.pptx` deck export with text, rich text ranges, image assets, masks where supported, shape backgrounds, speaker notes, and open review comments.
- Added native PDF deck export that renders each slide through the existing SVG/PNG export pipeline and downloads a landscape PDF.
- Added Turso-backed deck version history with bounded `deck_revision` snapshots, throttled autosave revisions, manual-save revisions, restore revisions, API routes, and a signed-in Version History dialog.
- Added configurable print handout setup with full-slide, notes-page, 2-up, 4-up, and outline layouts plus orientation, notes, comments, slide-number, and date-footer controls.
- Added a deck asset health panel with storage metrics, largest-image review, unused/duplicate/missing asset checks, and undoable cleanup for duplicate or unused image assets.
