# Essence PowerPoint Todo

Current project status: 80 / 100 for the thirty-first feature set.

The fourth, fifth, sixth, seventh, eighth, ninth, tenth, eleventh, twelfth, thirteenth, fourteenth, fifteenth, sixteenth, seventeenth, eighteenth, nineteenth, twentieth, twenty-first, twenty-second, twenty-third, twenty-fourth, twenty-fifth, twenty-sixth, twenty-seventh, twenty-eighth, twenty-ninth, and thirtieth feature sets are complete at 100 / 100. The thirty-first feature set is in progress at 80 / 100, focused on deeper native Office package parity, responsive workspace ergonomics, configured smoke execution handoff, advanced import/export repair loops, and presenter delivery polish while respecting the current lightweight-only verification constraint.

This is possible, but a true PowerPoint-class editor is a large product, not a weekend component. The current repo has a serious foundation and the first functional editor milestone. Most PowerPoint features are still missing.

## Feature Gap Against Microsoft PowerPoint

| Area | PowerPoint capability | Current repo status |
| --- | --- | --- |
| Core app shell | Ribbon, backstage file view, recent files, account, settings | Partial: editor shell, auth panel, searchable command palette, and a PowerPoint-style backstage file hub with Home/Open/Export/Recover/Info tabs wired to real file actions plus searchable, pinnable, stale-cleanable recent local decks, cloud deck shortcuts with sync-readiness cues, a document-readiness dashboard in the Info tab, an Export-tab PPTX preflight panel, and ODP intake reporting |
| File model | PPTX/PPT/POTX/PPSX/ODP support, recovery, compatibility mode | Partial: JSON import/export, PPTX import for slide text/notes/images/basic shape geometry/theme colors/tables/charts with compatibility warnings for skipped PowerPoint-only features, first editable ODP slide/text/notes/table/media/image/basic-shape/chart import with compatibility reporting for richer objects, local recovery snapshots with pre-restore review, slide-level change summaries, copy/download controls, native PPTX deck export, ODP package preflight reports, and Backstage readiness summaries for recovery, compatibility, import-report, asset, local-save, and cloud-account state |
| Deck CRUD | Create, rename, duplicate, delete, autosave, open recent | Partial: local editing plus authenticated cloud save/load/delete, debounced signed-in autosave, searchable/sortable cloud deck browser, pinned cloud decks, local recent-cloud-deck shortcuts, backstage cloud-shortcut open/pin/forget controls, and admin deck visibility/delete controls |
| Slide CRUD | Add, duplicate, delete, reorder, sections | Partial: add-after-current, selected-slide-set duplicate/delete/move, selected-slide-set toolbar/keyboard/filmstrip reordering, filmstrip slide action menu, current-section selection for section-wide operations, quick section creation and selected section-break clearing, current-section rename controls, whole-section move up/down actions, expand/collapse-all section controls, persisted collapse-state cleanup for real section breaks, section-aware export/print summaries, drag-and-drop filmstrip reordering, selection-free Alt+Arrow slide reordering, Ctrl/Command-click and Shift-click filmstrip multi-slide selection, filmstrip arrow/Home/End navigation and Ctrl+A/C/X/V/D/Delete keyboard workflows, toolbar and canvas whole-slide copy/cut/paste for selected slide sets, Ctrl+M slide creation, selection-free Ctrl+Shift+D slide duplication, PageUp/PageDown/Home/End canvas slide navigation, persisted section breaks, collapsible filmstrip sections, and shape-aware filmstrip thumbnails; deeper drag handles and section metadata are still missing |
| Canvas | True WYSIWYG slide editing, rulers, guides, grid, snap, zoom | Partial: selectable canvas, direct text-object editing, zoom, status-bar zoom slider/reset and selection telemetry, canvas zoom shortcuts, persisted slide grid and ruler toggles, in-app object cut/copy/paste with Ctrl+X/Ctrl+C/Ctrl+V, external clipboard image/text paste into slide objects, drop-positioned drag-and-drop image/text insertion, selected-object duplication with Ctrl+D, format-painter style copy/apply with Ctrl+Shift+C/Ctrl+Shift+V shortcuts, canvas Ctrl+Z/Ctrl+Y undo-redo shortcuts, Ctrl+A object selection, Escape selection clearing, Tab/Shift+Tab object traversal, Ctrl+Bracket layer stepping and front/back shortcuts, Ctrl+G/Ctrl+Shift+G grouping shortcuts, persistent grouping/ungrouping, group drag, marquee select, eight-handle single-object resize, eight-handle multi-object transform resizing with shared rotation, drag rotation handle with readout, keyboard nudge, multi-selection front/back layer ordering, layer pane additive selection and step-order controls, layer lock/hide controls, rotation, PPTX object rotation import, PPTX grouped-object import, alignment, distribution, slide edge/center snap guides, object edge/center snap guides, connector start/control/end point editing through the properties panel and direct canvas drag handles, and dense-slide canvas render budgeting that preserves selected text checks while reducing unselected decoration work |
| Text editing | Rich text ranges, bullets, numbering, columns, alignment, line spacing | Partial: direct canvas text editing, whole-object text with alignment, line height, bullet lists, numbered lists, columns, shrink-to-fit overflow, clipped-text indicators, selection-based inline bold/italic/underline/color ranges, toggle-aware range controls, and range splitting for partial clears |
| Shapes | Full shape library, lines, connectors, boolean ops, edit points | Partial: editable rectangle, rounded rectangle, ellipse, diamond, triangle, pentagon, hexagon, parallelogram, trapezoid, right arrow, chevron, plus, star, speech bubble, line, arrow, double-arrow, elbow connector, and curved connector shapes with toolbar gallery insertion, PPTX import mapping for common Office preset shapes, fill/stroke/dash/arrowhead controls, start/end arrowhead controls, explicit connector/line start-control-end geometry, direct edit-point handles, canvas/slideshow rendering, cloud validation, accessibility checks, SVG/PNG/PDF export, import normalization, native PPTX export for the non-connector shape catalog including speech-bubble callouts, routed connector export as editable custom Office geometry with speaker-note route handoff cues, and a Backstage object-conversion report for live connector-binding risks; true object-bound Office connector relationships, boolean ops, and the full Office shape catalog are still missing |
| Images | Upload, crop, mask, remove background, corrections, filters | Partial: image insert/replace, image-as-slide import, compressed deck-level image asset library, persisted image assets, cloud manifest delivery for large saved image assets, authenticated asset content streaming, asset health and cleanup panel, contain/cover/fill, crop position controls, rectangle/rounded/circle/diamond masks, opacity plus brightness/contrast/saturation corrections in editor, slideshow, SVG/PNG/PDF export, cloud validation, and style copy/paste |
| Video/audio | Insert, trim, playback, captions, recording | Partial: local video and audio insertion/replacement as editable slide objects with canvas and slideshow playback, slideshow autoplay option, start/end trim controls, editable plain captions, timed VTT/SRT caption cue parsing, generated browser caption tracks for playback, caption cue summaries in static exports, PPTX speaker-note caption handoff metadata, native PPTX media embedding for supported inline MP4/M4V/MOV/MP3/M4A/AAC/WAV sources, PPTX import for supported embedded MP4/M4V/MOV/MP3/M4A/AAC/WAV media as editable playback objects, ODP embedded audio/video plugin import for supported package media as inline editable playback objects, typed desktop/browser-local media resolver handoff for packaging local and blob media into native PPTX media, media source compatibility/readiness reporting, native-vs-fallback PPTX export reporting, a real browser recording dialog for microphone audio, camera video, and screen capture insertion or selected-source replacement, capture-mode readiness checks, deterministic recorded media filenames, trim/caption alignment review, presenter handoff summaries, validation, accessibility checks, SVG/PDF/PPTX fallbacks, and fit controls for video; native PowerPoint trim editing remains a handoff |
| Tables | Table insert, style, merge/split cells, sizing | Partial: insert/edit table objects with rows, columns, quick row/column add-remove controls, header row, total row, banded rows, banded columns, first/last column emphasis, horizontal and vertical table text alignment, cell text, fill, borders, plain/accent/dark/minimal/grid/executive style presets, merge-header and split-all cell structure controls, coordinate-based arbitrary cell range selection, selected-range TSV editing, selected-range merge/split, selected-range fill/border/text/weight overrides, selected-cell border-side variants for all/outer/inner/single-side/none border workflows, imported Office table style metadata retention, normalized merged-cell metadata, canvas/slideshow/SVG rendering for spans and side borders, cloud validation, SVG/PNG/PDF export, native PPTX table export with row/column spans and side borders when not rotated, rotated-table visual fallback with side-border preservation, PPTX table import with merged-cell and cell fill/border preservation, and ODP `table:table` import as editable Essence tables with frame geometry, simple span metadata, table-cell fill/text/bold/border preservation, and style-backed selected-cell metadata; richer ODP table presets and richer Office table style mapping are still missing |
| Charts | Native charts, Excel data editing, linked data | Partial: insert/edit basic bar, horizontal bar, line, area, pie, and donut chart objects with typed data, labels, values, legend toggles, axis color, explicit multi-series TSV editing, category-aligned series colors, multi-series canvas/slideshow rendering, cloud validation, accessibility checks, SVG/PNG/PDF export, native editable multi-series PPTX chart export for non-rotated charts, rendered PPTX fallback for rotated charts, PPTX cached chart import with series preservation, ODP chart-object import from local chart tables as editable Essence charts with preserved multi-series labels/values and chart-data preflight summaries, and reusable linked-data repair workflows for unsupported linked chart sources and rotated chart fallbacks; true linked Excel/ODP workbook round-trip data and richer chart styling are still missing |
| SmartArt/diagrams | Hierarchy, process, cycle, matrix, pyramid, relationship diagrams | Partial: quick editable grouped diagram gallery for process, cycle, hierarchy, matrix, pyramid, and relationship layouts, diagram groups carry source-template ids through insert/copy/duplicate flows, and PPTX preflight snapshots now expose PowerPoint-friendly layout metadata for future SmartArt rebuilds; SmartArt style variants and text-driven diagram conversion missing |
| Equations/symbols | Equation editor, symbols, math formatting | Partial: symbol and equation-snippet gallery inserts editable slide text objects; full equation editor, math layout engine, and advanced symbols missing |
| Icons/3D/models | Icons, stickers, 3D models, stock media | Partial: built-in editable SVG icon gallery with canvas/slideshow rendering, title metadata, color/stroke controls, cloud validation, SVG export, and PPTX SVG embedding; grouped editable sticker templates added; richer sticker catalog, stock media, and 3D models missing |
| Theme system | Themes, variants, font pairs, color palettes | Partial: app chrome themes plus a design palette gallery that recolors the current slide or whole deck using existing slide/object styles, local custom design palettes saved from the current deck, reusable saved palette apply/delete controls, portable custom-palette JSON import/export, recommended custom palettes, font-pair presets that update title/body objects plus deck master defaults, reusable brand kits with combined palette/font previews and deck master typography defaults, custom brand-kit save/import/export for portable visual systems, recommended custom brand kits, selectable template theme bundles that preview and apply palette/font-pair/master font defaults before creating a deck, saved custom theme bundles that capture palette, fonts, and full master defaults with JSON import/export, usage metadata, stale/unused/duplicate cleanup actions, standalone Essence theme-file export/import carrying paired theme bundles, Office theme metadata, package manifests, and authored theme XML readiness, deterministic master-layout Mirror/Stack variants linked to saved master presets, a master/theme safeguard report that audits metadata gaps before risky reapply work, owner-visible Office theme variant repair actions for metadata, source review, `.thmx` package XML, master variants, and placeholder inheritance, PPTX Office theme metadata preservation for theme name, color scheme, major/minor fonts, master/layout counts, and placeholder default counts with native PPTX font export, plus reusable theme-file handoff planning for native master XML; true binary `.thmx` variant editing remains incomplete |
| Slide layouts | Title, section, comparison, blank, custom placeholders | Partial: persisted layout labels plus non-destructive placeholder application for title, title/body, section, blank, two-content, comparison, quote, and picture-caption layouts; layout-applied objects now carry editable title/body/media/caption placeholder roles, layout reapply prefers matching placeholders, locked matching placeholders reserve their layout slots without duplicate replacements, users can save/apply/delete local custom layouts captured from the current slide's tagged placeholder geometry, saved custom layouts can be applied to either the current slide or the selected slide set through one history-safe operation, custom slide-layout JSON import/export makes placeholder systems portable, and named deck-master layout presets can be saved/applied/deleted, previewed, edited in place for preset name/description/slot geometry/slot text, imported, exported, usage-counted, sorted by recent use, recommended on blank slides from recent-use metadata as presentation data, analyzed for native PPTX layout placeholder coverage in export preflight, and listed as explicit `ppt/slideLayouts/*` package-part plans; PPTX slide-layout import maps imported slides to the closest Essence layout and inherits missing placeholder geometry/font defaults; true Office slide-layout XML authoring is still missing |
| Slide master | Master slides, layouts, placeholders, footer/date/number | Partial: deck-level master settings for footer text, date, slide numbers, color, font size, and font family render in editor, slideshow, SVG/PNG/PDF, handouts, and PPTX export; deck master now has reusable style presets with footer/date/number placement previews for common treatments, portable custom master-style presets for house footer systems, named layout presets with placeholder geometry that can be previewed, edited in place for label/description/slot geometry/slot text, imported/exported as JSON, applied to the current slide or selected slide set, tracked by use count/last-used timestamp, recommended for blank-slide authoring, and survives deck normalization/server validation, linked Mirror/Stack variants generated from saved master geometry, locked placeholder slot preservation during built-in and custom layout reapply, plus a Master safeguards panel that reports missing master presets, uncovered placeholder roles, manual placeholder overrides, locked placeholders, stale/duplicate master presets, Office theme color/layout/default coverage, metadata gaps, and bulk reapply risk slides; PPTX import can inherit slide-layout and master backgrounds/placeholders; PPTX export preflight now counts footer/date/slide-number defaults, native layout candidates, layout-slot handoffs, theme fonts, theme colors, explicit native master XML package parts, theme-file handoff status, and placeholder inheritance diagnostics before deeper Office master XML generation; broader true master-slide editing is still missing |
| Templates | Built-in templates, custom templates, theme files | Partial: built-in editable deck template gallery for startup pitches, project updates, and lesson plans, selectable built-in theme bundles with live template preview thumbnails, local custom templates saved from the current deck with paired captured theme bundles, portable custom-template JSON import/export, template package manifest export with stale/unused/unpaired/mismatched-theme diagnostics, custom template usage/freshness stats with imported/stale/paired-theme counts, reusable recommendations that prefer fresh paired-theme templates with usage history, a reusable asset audit with direct cleanup actions for stale/unused/unpaired templates and master layouts, reusable brand kits for deck-wide visual systems, portable custom brand-kit JSON files, portable theme-bundle JSON files with stale cleanup controls, standalone Essence theme-file import/export workflows, and imported PPTX Office theme metadata retained on deck masters; direct binary `.thmx` import/export remains a handoff |
| Designer ideas | Design suggestions, layout repair | Out of scope for non-AI phase |
| Transitions | Slide transitions, timing, sound, morph | Partial: none/fade/push/zoom, visual preset thumbnails, duration, editor preview, auto-advance timing, PPTX import for common transition type/duration/auto-advance metadata, ODP import for common transition type/speed/page-duration metadata, native PPTX transition XML injection for supported transition/timing export, and PPTX speaker-note handoff cues for review; transition sounds, morph, and advanced variants are still missing |
| Animations | Entrance/emphasis/exit/motion paths, animation pane | Partial: per-object animation settings for none/fade/rise/zoom/wipe/fly-left/fly-right entrance effects, pulse/spin/grow-shrink/teeter emphasis effects, fade-out/fade-out-down exit effects, motion-left/motion-right/custom path effects with duration, delay, custom X/Y offsets, and on-click/with-previous/after-previous trigger choices, editor preview, a real animation pane for ordered slide effects, selected-object add/remove, effect/timing/trigger/custom-motion edits, preview, reorder, slide-level timing audits, slideshow sequencing that keeps on-click effects in the click sequence while auto-playing with/after-previous effects, native PPTX animation timeline XML for stable entrance, emphasis, exit, motion-path, and custom-motion targets, and PPTX speaker-note review cues that preserve effect order, effect kind, native-vs-review support path, duration, delay, trigger, and motion offsets; richer Office animation variants and path editing handles are still missing |
| Links/actions | Hyperlinks, action buttons, slide links | Partial: object action URLs for http, https, mailto, and tel links plus internal slide jumps, quick action-button gallery with selectable action-button style variants, styled action-button payloads, selected-object slide-jump diagnostics, copyable PPTX action-setting handoff reports, native PPTX action overlay and relationship XML authoring for safe slide jumps and URL actions, PPTX preflight action metrics, properties-panel testing, slideshow click-through, accessibility warnings, and SVG/PPTX export links; full Office action catalog and deeper user-editable custom action styling are still missing |
| Presenter tools | Presenter view, speaker notes, timer, laser, ink, subtitles | Partial: notes, next-slide preview, direct slide navigator, second-window audience display, timer, laser pointer, ink, synced media caption toggle, timed caption cue playback through native media tracks, black/white screen blanking, persistent rehearsal timings, and a presenter-readiness panel that reviews rehearsal coverage, total/average/longest timing, speaker-note gaps, short presenter notes, plain or timed media captions, auto-advance mismatches, audience-display reliability, fullscreen support, blank-screen handoff state, and media trim/caption presenter handoff summaries; readiness rows can jump to problem slides and apply rehearsed timings into auto-advance settings, and the panel now groups cleanup actions for rehearsal drift, missing rehearsals, note polish, missing captions, and delivery-screen handoff blockers |
| Slideshow | Fullscreen playback, keyboard navigation, timings | Partial: fullscreen-capable playback overlay, keyboard navigation, second-window audience display synced from presenter controls, direct progress scrubbing, synced captions toggle for media subtitles, click-through per-object animation sequencing, black/white screen blanking, auto-advance, persistent timing, basic transitions, per-object entrance playback, and runtime presenter-readiness cues for delivery blockers plus actionable slide-level rehearsal review |
| Review | Comments, compare, spellcheck, thesaurus, accessibility checker | Partial: slide/object comments with open/resolved state, mention extraction, deck-wide review queue with previous/next navigation, owner-copyable collaboration handoff reports, richer reply-control metadata for manual and imported PowerPoint threads, PowerPoint comment-thread/reply/manual-note export preflight metrics, native comment-thread XML planning with anchor readiness, version comparison against saved snapshots, canvas/status badges, local/cloud persistence, conflict detection, printable handout comments, real accessibility issue checker, and deck proofing scanner for common typos, repeated spacing, and repeated punctuation |
| Collaboration | Sharing, presence, coauthoring, mentions, version history | Partial: database-backed version history with manual, autosave, restore snapshots, saved-version comparison, mention-aware review comments, deck-wide review navigation, tested mention matching and notification preview policies, mention-targeted notification routing for verified users, authenticated deck-presence heartbeat tracking with active-editor indicators, tested presence heartbeat normalization, owner-managed read-only share links with optional expiry, access codes, and download controls, share view activity tracking, tested share-view dedupe windows, durable owner notifications for shared-deck views, tested share-link lifecycle validation, version restores, an authenticated notification inbox, public shared-deck viewer with notes, outline, open-comment review, owner-controlled PDF/PPTX downloads, shared-view review snapshots for download permission, unresolved comments, mentions, PPTX threads, and note coverage, copyable shared-link review reports that serialize unresolved comments, mention labels, PPTX thread IDs, slide references, and download permission state, owner handoff exports that classify unresolved comments into PowerPoint root threads, reply threads, speaker-note handoffs, and role-aware merge/share/export policy, role-aware review-thread lifecycle grouping for open/mixed/resolved manual and PPTX threads, richer per-thread reply controls with parent/depth planning, safer cloud merge summaries that block viewer merges while grouping conflict areas for owner/editor review, explicit merge-resolution choices for clean merge, inspect conflicts, keep-local, use-cloud, and owner-request flows, audit-ready collaboration operation summaries, pure three-way deck merge groundwork, conflict-panel merge action, granular merge/conflict review UI, server-side merge persistence for local/cloud edits, a tested deck-operation access policy for owner/editor/viewer/anonymous collaboration roles, database-backed private collaborator membership, owner-managed collaborator APIs, share-dialog collaborator management, collaborator-aware cloud deck listing, editor/viewer access routed through deck read/save/merge/presence paths, role-aware cloud/editor chrome that hides owner-only or writer-only actions from viewers, durable notifications for collaborator add/update/remove permission changes, active presence role badges for owner/editor/viewer context, pending collaborator invitations that auto-accept into active access when the invited email signs in, role context in version history plus cloud conflict/merge review, a permission-checked collaboration event ledger for cursor, selection, edit-intent, and object-mutation operation envelopes, useful presence-menu operation summaries, live canvas cursor publishing/rendering for recent remote collaborator pointers, remote selection/edit-intent overlays for selected objects, throttled remote object-mutation ghost overlays for move/resize/rotate/text edits, a history-safe accept path for applying compatible remote object snapshots while skipping locked/missing/type-conflicting elements, and canvas feedback that summarizes applied versus skipped remote changes with jump targets for skipped local objects; full CRDT-style model reconciliation is still missing |
| Cloud sync | OneDrive/SharePoint style autosave, conflict handling | Partial: authenticated manual save/load/delete, deck-level asset persistence, optional S3/R2-compatible object-store persistence for large assets behind authenticated content routes, manifest-style large-asset delivery with owner-only content routes, debounced autosave, remote-change conflict guard, local-vs-cloud conflict preview, last-synced base tracking, clean three-way merge action, granular merge/conflict review UI, server-side merge endpoint, merge regression coverage for identical dual edits, local deletions, and duplicate asset-id conflicts, stable browser selectors, typed e2e flow contracts, disabled-control assertions for review-only merge states, DOM and locator browser drivers, an always-on credential-free e2e readiness gate for selector/flow/env validation, optional Playwright smoke script, and manual CI workflow for conflict preview/merge/open/overwrite/deck-row/review-only flows, explicit open-cloud/overwrite conflict actions, explicit merge-resolution choice metadata for owner/editor/viewer roles, version restore, and backstage cloud shortcuts that notify the cloud panel after opening so autosave/conflict tracking remains coherent; live always-on browser execution is intentionally deferred until stable test credentials are available |
| Export | PDF, images, video, GIF, handouts, print layouts | Partial: JSON deck export, native PPTX deck export including basic shapes, native editable tables with merged-cell spans and selected-cell side borders when not rotated, editable charts where supported, editable custom geometry for routed connectors, supported inline and desktop-resolved audio/video as native PPTX media, native transition/timing XML for supported slide transitions, native animation timeline XML for stable entrance/emphasis/exit/motion effects, animation cue review notes, connector route handoff notes, transition/timing handoff notes, media export fallback notes, and timed media-caption VTT handoff notes, native PDF deck export, selected-slide SVG/PNG export, configurable printable handout HTML with section overview data, owner-controlled PDF/PPTX downloads from shared view links, Backstage export-readiness checks for active compatibility or asset blockers, Backstage section handoff summaries for print/export review, Backstage object-conversion reporting for PowerPoint handoff editability, and grouped copy/download PPTX preflight snapshots for native master/layout readiness, chart/table fidelity, media packaging decisions, native media counts, desktop-resolved media counts, media fallback placeholders, media source candidates, media trim handoffs, timed caption cues, native-animation review cues, connector route cues, transition timing cues, native tables, merged cells, imported Office table style metadata, selected-cell border variants, slide timings, chart fallback cases, image effects, comments, comment-thread anchor readiness, explicit master XML part readiness, theme-file handoff readiness, placeholder inheritance readiness, groups, hidden objects, lists, columns, and master flattening |
| Import | PPTX, ODP, images as slides, outline import | Partial: JSON import, PPTX import for slide order, text boxes, speaker notes, basic backgrounds, theme colors, common Office preset shapes/connectors, embedded images, supported embedded audio/video media, tables including merged-cell row/column spans, imported Office table style ids, cell fill/text/border-side styling, cached charts, object rotation, grouped-object membership with group coordinate mapping, slide layout/master placeholder inheritance, common slide transition timing/type metadata, comment text/author/timestamp/anchor/thread-id/parent/reply-depth preservation for legacy and basic threaded PPTX comment parts, persisted reusable File-menu compatibility history, image-as-slide import with staged review/reorder/remove controls, orientation summaries, dimensions, duplicate-name warnings before insertion, a persisted last-import report with inserted slide range, rendered current-deck thumbnails, copy/download/clear actions, per-slide jump targets, File-menu surfacing for the latest image-import report, a dedicated PPTX review dialog with copy/download report controls for longer warning reports covering animations, unsupported media, embedded objects, ActiveX controls, SmartArt, macros, unsupported rich conversation metadata, and unsupported transition details that cannot yet be preserved, plus ODP package preflight and editable ODP import for slide order, title/body text, tables with cell fill/text/bold/border style preservation, supported embedded media, common transition metadata, basic drawing shapes, embedded package images, common local-table charts including flattened multi-series values, and speaker notes with animation warning metadata; linked chart data, advanced chart styling, and deeper ODP preset mapping are still missing |
| Printing | Notes pages, handouts, outline, page setup | Partial: configurable printable handout HTML with full-slide, notes-page, 2-up, 4-up, and outline layouts plus orientation, metadata toggles, named-section overview pages, section markers, section-grouped outlines, and slide master footer/date/number rendering |
| Security | Permissions, protected view, password encryption, sensitivity labels | Partial: authenticated deck ownership, owner-managed private editor/viewer collaborators, pending invite state, admin-only management APIs, disabled/expired share-link enforcement, access-code protected shares, viewer download controls, and email verification are wired; deck encryption, richer external permission auditing, and sensitivity labels are missing |
| Add-ins/macros | Office add-ins, VBA/macros, extensibility | Missing |
| Desktop integration | Native file open/save, menus, recent files, OS dialogs | Partial: hosted-web Tauri shell profile, desktop packaging preflight, shell scaffold, Rust-installed native File/Import/Export/Recover menu dispatching the existing web command IDs, picker-assisted deck/PPTX/outline/image-slide import, picker-assisted `.essdeck` plus legacy JSON deck saves, dialog-backed Rust/Tauri commands for scoped deck read/write, presentation and outline import, image-slide import, export writes, and current/native recent deck saves, Ctrl/Command+O/S file shortcuts, same-file `.essdeck` save handles with legacy `.json` open compatibility, native-path current-file save reuse, a File menu with clean/dirty local-file status, read-only fallback import guidance, scoped local recovery snapshots with current-deck cleanup and older-deck separation, IndexedDB-backed recent local deck handles plus native-path recent metadata with search, pin/unpin, individual forget, stale cleanup, readiness warnings, a typed desktop menu command contract with browser/native/fallback channel reporting, permission-scoped desktop file command payloads for open/save/import/export/recover actions, recent-file native handoff metadata surfaced in the local-file status dialog, a typed desktop media resolver export handoff for local/blob media packaging, an event bridge that native Tauri menus dispatch into the existing File actions, installable Tauri `.essdeck` file-association metadata that does not claim generic `.json`, OS recent-document writer/registration metadata, signed-package readiness checks, native open/save edge-case coverage, desktop release-registration preflight checks, and a desktop packaging panel in Local file status; signed artifacts and installer store submission remain missing |
| Offline mode | Full local editing without cloud dependency | Partial: localStorage deck state plus scoped local recovery snapshots with current-deck cleanup, pre-restore snapshot review, rollback checkpoint capture before restore, slide-level change summaries, and portable snapshot JSON backup |
| Auth/accounts | Email/password auth, sessions, user-owned decks | Partial: email/password sessions, Brevo-backed email confirmation codes, seeded admin account support, admin dashboard access, session visibility/revocation, and user-owned deck APIs are wired |
| Database persistence | Deck/slide migrations, sync | Partial: database migrations, deck APIs, admin/auth/deck data health checks, production data-operation runbooks, stale share/deck cleanup planning, asset storage hygiene checks, and owner-visible remediation rows are ready |
| Testing | Unit, integration, e2e, editor regression fixtures | Partial: runnable Bun presentation regression harness covers default deck shape, outline import, image-slide import review summary/reorder/remove/orientation/duplicate-name behavior, image-slide inserted-range report serialization, links, styled action-button payloads and variants, action-setting handoff reports, rich text toggles, comment mentions, image asset dedupe/migration/compaction, undo-history trimming, local deck file clean/dirty status, recent local deck pin/search/stale grouping, desktop bridge readiness summaries, desktop menu command contract channels, permission-scoped desktop file payloads, native desktop read/write command invocation contracts, desktop packaging readiness/open-save edge cases, local recovery snapshot dedupe/deck-scoped pruning/pre-restore comparison/restore-checkpoint behavior/slide-delta summaries/text summaries/JSON download filenames, PPTX compatibility-warning summaries, PPTX transition metadata import/export XML injection, PPTX native animation timing XML injection for entrance/emphasis/exit/motion effects, PPTX custom connector geometry export, PPTX native media package entries and relationship checks, PPTX comment text/author/mention/anchor/thread-id/parent/reply-depth parsing, persisted report parsing, text report serialization/download filenames, and multi-import compatibility history ordering/selection, cloud deck organization, backstage cloud shortcut readiness, backstage document readiness summaries, shared-view review snapshot and report serialization, structured PPTX preflight snapshot sections, PPTX export preflight summaries, native PPTX master/layout readiness metrics, native PPTX master XML package planning, theme-file handoff readiness, placeholder inheritance diagnostics, native PPTX chart export-mode decisions, multi-series chart edit/export round-trips, PPTX animation handoff cue serialization/support classification, animation pane ordering/summary/reorder behavior, advanced animation sequence visibility/playback rules, PPTX connector route handoff serialization, PPTX transition handoff cue serialization, table merge/span rendering and validation normalization, selected table range editing/formatting/merge/split behavior, selected table border-side variants, Office table style metadata retention, ODP table/media/transition/chart preflight/import behavior including preserved multi-series local chart tables, timed media caption parsing/serialization/PPTX handoff/readiness counting, media handoff source classification/native-candidate/native-PPTX-mode/recording-readiness reporting, recording capture mode readiness/MIME/filename helpers, master/theme safeguard reporting for reapply risk and metadata gaps, object conversion handoff summaries, ODP package preflight summaries, ODP editable slide/text/notes import, connector geometry routing, canvas edit-point projection, drag-patch clamping, validation clamps, section rename/move/collapse-state helper behavior, section export summary metrics, print section overview escaping/ranges, presenter-readiness rehearsal/notes/handoff status, presenter slide-review timing and note-polish issues, presenter timing-action readiness fields, conflict previews, three-way deck merge outcomes, merge-resolution choices, collaboration operation audit summaries, thread reply-control depth planning, cloud-sync e2e selector contract validation, cloud-sync e2e readiness reporting, cloud-sync e2e runner and locator-driver execution order, production browser smoke contracts for status-bar pressure, Backstage export, and desktop packaging surfaces, production pressure diagnostics, server deck-payload validation including PPTX comment hierarchy metadata, admin access policy decisions, admin user-update policy decisions, deck share-update policy decisions, deck operation access-policy decisions, collaborator payload normalization, collaboration event envelope validation, collaboration cursor projection, collaboration selection/edit-intent projection, collaboration object-mutation projection, safe reconciliation, skipped-object details, collaboration activity labels, canvas render budgeting, custom design palette derivation/recommendation and JSON round-trip parsing, template theme bundle creation, custom template paired-theme capture/reapply/import/export/usage/freshness/recommendation/audit behavior, custom theme-bundle capture/import/export/apply/usage/cleanup behavior, master style preset/preview/custom import-export behavior, deck-master layout preset editing/variant behavior, font-pair application and validation defaults, brand-kit palette/font/master application, custom brand-kit capture/import/export/recommendation parsing, layout placeholder role application, custom slide-layout capture/apply/bulk-apply/import/export JSON parsing, deck-master layout preset save/normalize/delete/import/export/usage/recommendation behavior, presence heartbeat/initials policy decisions, notification mention/preview/dedupe policy decisions, store persistence hydration safeguards, editor selection geometry commands for nudge/marquee/align/distribute/resize/rotate, export filename/SVG/print-handout serialization edge cases, always-on CI e2e readiness, and manual CI browser smoke execution for prepared cloud-sync environments; live browser e2e still needs stable credentials and low-cost hosted fixtures |
| Accessibility | Keyboard editing, ARIA review, contrast, screen reader flow | Partial: keyboard editing, basic labels, editor landmark roles, canvas/layer object labels, focus and keyboard parity reporting, shortcut coverage checks, alt-text checks, contrast checks, clipped-text checks, empty-text checks, and jump-to-issue review panel |
| Performance | Large deck virtualization, asset caching, undo memory limits | Partial: deck-level image asset dedupe, optional object-store offload for large saved images, large saved images delivered through authenticated remote asset URLs, configurable stale remote-object deletion/retention, asset health checks, duplicate/unused asset cleanup, filmstrip thumbnail windowing, dense-slide canvas render budgeting, undo/future history memory budgeting, deck/history size telemetry, deck scale metrics including animation-density pressure, large/heavy deck status warnings, production pressure diagnostics for deck size, busiest slides, animation density, undo pressure, dense slides, and budgeted slides, plus regression stress coverage; broader virtualized canvas rendering is still missing |

## Thirteenth Feature Set

Current status: 100 / 100.

1. Completed a PowerPoint-style backstage hub with Home/Open/Export/Recover/Info tabs wired to real file actions.
2. Completed recent local and cloud deck shortcut management with search, pinning, forgetting, stale cleanup, sign-in readiness, and autosave/conflict tracking handoff.
3. Completed production-readiness surfaces for deck size, assets, permissions, recovery state, compatibility reports, import reports, and export readiness.
4. Completed PPTX export preflight for known lossy handoff risks.
5. Completed ODP investigation groundwork with real package preflight reporting instead of fake unsupported-format UI.

## Fourteenth Feature Set

Current status: 100 / 100.

1. Completed stronger section management with current-section rename controls, whole-section move up/down actions, persisted collapse-state cleanup, Backstage export section summaries, and section-aware printable handouts/outlines.
2. Completed presenter readiness workflow actions with a slideshow readiness report and panel for rehearsal coverage, total/average/longest timing, speaker-note gaps, short-note polish warnings, media captions, auto-advance mismatches, audience-display status, fullscreen support, blank-screen handoff checks, issue-slide jumps, and use-rehearsed-timing actions.
3. Completed native-adjacent desktop menu/file contracts with typed File/Import/Export/Recovery command IDs, browser/native/fallback channel reporting in Local file status, selected-slide export blocking, and a custom event bridge future Tauri menus can dispatch while browser fallbacks remain intact.
4. Completed PowerPoint object handoff reporting with deck-wide conversion metrics for native-like objects, simplified editability, placeholders, hidden/skipped objects, connectors, custom shapes, media, charts, icons, tables, groups, animations, image effects, and text-layout flattening.
5. Completed the export-side round-trip metadata slice by surfacing object conversion risks in Backstage Export; editable ODP conversion moves into the fifteenth feature set.

## Fifteenth Feature Set

Current status: 100 / 100.

1. Completed the first editable ODP slide/text conversion path after the current package preflight, preserving slide order, titles, body text, speaker notes, and image/media/chart/animation/transition warning metadata in the stored ODP report.
2. Completed the first connector/edit-point object geometry pass with explicit start/control/end routing data, property-panel controls, direct canvas drag handles, normalized server/cloud validation, PPTX import defaults, canvas/SVG/PPTX rendering, and PPTX speaker-note route handoff cues; native Office connector edit-point export still needs deeper work.
3. Completed the PPTX round-trip metadata slice by exporting non-rotated Essence charts as native editable Office charts, writing animation handoff cues into PPTX speaker notes with effect order, effect type, duration, delay, and click-trigger instructions, exporting connector route handoff cues with start/control/end points, arrowheads, and dash style, writing transition/timing handoff cues for slide rebuild, and exporting the non-connector Essence shape catalog including speech bubbles as native Office shapes; native animation timelines and connector edit-point metadata move into the next native XML feature set.
4. Completed the table merge/split structure slice with normalized merged-cell metadata, merge-header and split-all controls, canvas/SVG rendering for spans, server validation, PPTX import preservation, native PPTX table serialization for non-rotated tables, and rotated-table visual fallback.
5. Kept native desktop integration ready for Rust-side menu/file API depth once Tauri command checks are allowed again, with browser fallbacks intact.

## Sixteenth Feature Set

Current status: 100 / 100.

1. Completed a verified native PPTX slide transition XML exporter adapter for supported fade/push/zoom/timing settings while preserving the existing speaker-note fallback for review.
2. Completed native PowerPoint animation timeline authoring for supported entrance effects, with notes remaining as compatibility handoff for timing review and unsupported advanced variants.
3. Completed routed connector export as editable custom Office geometry for elbow and curved connectors, preserving route points, stroke dash, and arrowhead metadata while keeping handoff notes for live object-bound connector rebuilds.
4. Completed the first Rust-side Tauri native menu bridge with File, Import, Export, and Recover menus that dispatch the existing web command IDs into the editor, covered by targeted Rust unit tests and desktop preflight.
5. Completed an always-on credential-free cloud-sync e2e readiness gate with selector/flow/env/storage-state validation, a Bun script, CI pull-request/push workflow wiring, and regression coverage; live browser execution stays manual until stable test credentials are available.

## Seventeenth Feature Set

Current status: 100 / 100.

1. Completed a real animation pane for ordered object effects, timing edits, selected-object add/remove, preview, reorder workflows, and slide-level playback timing audits backed by regression-covered ordering helpers.
2. Completed emphasis, exit, and motion-path animation primitives with pulse, spin, fade-out, and motion-right effects, safe slideshow sequence visibility/playback rules, preview cleanup, persisted validation support, and native PPTX XML limited to reliable entrance effects while handoff notes keep advanced effect metadata.
3. Completed safer placeholder-lock behavior, linked master layout variants, and an editable master-preset surface for label, description, slot geometry, and slot text; deeper true master/theme safeguards move into the eighteenth feature set.
4. Completed coordinate-based table cell range selection, selected-range TSV editing, selected-range merge/split, selected-range fill/border/text/weight formatting, and grid/executive table presets that render through the existing canvas/export path; richer Office table metadata and deeper import refinements move into the eighteenth feature set.
5. Completed richer caption workflows with plain captions plus timed VTT/SRT cue parsing, generated playback caption tracks, caption cue summaries in static export placeholders, presenter-readiness caption counting, PPTX speaker-note handoff metadata, media handoff/source compatibility reporting, PPTX preflight media metrics, and browser recording API readiness; native PPTX media embedding implementation moves into the eighteenth feature set.

## Eighteenth Feature Set

Current status: 100 / 100.

1. Completed the first true master/theme safeguard workflow with a reusable report for theme-file metadata gaps, master inheritance coverage, placeholder override warnings, locked placeholder warnings, duplicate/stale master preset signals, and bulk reapply review risk slides surfaced in the Master panel.
2. Advanced structured table fidelity with imported Office table style metadata, PPTX cell fill/text/border-side import, selected-cell all/outer/inner/single-side/none border controls, side-border rendering in canvas/SVG/PPTX, validation persistence, and PPTX preflight metrics; deeper ODP table conversion and richer Office table style mapping remain.
3. Completed the first native media embedding path for supported inline audio/video sources, with PPTX package media entries, native-vs-fallback preflight metrics, source compatibility decisions, and speaker-note fallback reasons for remote/blob/local/rotated media that still need handoff.
4. Completed a real capture workflow for microphone audio, camera video, and screen recordings with safe local object insertion, browser readiness gates, MIME selection, preview/discard/insert controls, and deterministic recorded media filenames.
5. Completed PPTX embedded media import for supported MP4/M4V/MOV/MP3/M4A/AAC/WAV assets as editable playback objects, skipped media-cover image duplication, and made compatibility warnings distinguish supported media import from unsupported media handoff.

## Nineteenth Feature Set

Current status: 100 / 100.

1. Advanced deeper ODP conversion beyond slide text and notes with embedded `draw:image` and `Pictures/*` imports as editable image objects, plus basic `draw:rect`, `draw:ellipse`, `draw:line`, `draw:connector`, and common `draw:custom-shape` imports as editable Essence shapes with ODP frame/style mapping and updated preflight scope messaging.
2. Completed a typed desktop/local media resolver handoff for browser-local and file-reference sources, letting resolved base64 media flow into native PPTX packaging, preflight metrics, compatibility notes, and regression-covered package entries without broadening Tauri permissions yet.
3. Preserved true PPTX master/theme metadata by importing Office theme names, color schemes, major/minor fonts, slide-master/layout counts, and placeholder default counts into the deck master, keeping them through server validation and custom theme normalization, surfacing them in Master safeguards, and using imported major/minor fonts for native PPTX export.
4. Added richer recording and media editing controls with a reusable trim/caption/presenter handoff review model, media-panel editing status, selected matching audio/video recording replacement, trim reset on replacement, and regression coverage for caption alignment and replacement policy.
5. Hardened production workflows with deterministic PPTX preflight snapshots that can be copied/downloaded from Backstage Export, plus mixed-feature regression coverage for media, charts, tables, comments, transitions, master overlays, and stable snapshot filenames.

## Twentieth Feature Set

Current status: 100 / 100.

1. Started deeper native PPTX master/layout export with a typed readiness model for footer/date/slide-number defaults, native layout placeholder candidates, unsupported slot handoffs, Office theme fonts, Office theme colors, and export-preflight metrics while keeping the current slide-level flattening warning honest.
2. Advanced deeper ODP conversion by importing `table:table` objects as editable Essence tables, supported `draw:plugin` package audio/video as editable inline playback objects, common ODP transition type/speed/page-duration metadata as Essence slide transitions, and common local-table chart objects as editable Essence charts, with compatibility reports that keep richer table styles, multi-series chart data, linked chart data, and advanced chart styling honest as remaining gaps.
3. Upgraded advanced animation parity with typed handoff reports that distinguish native PPTX XML entrance effects from speaker-note-only emphasis, exit, and motion-path effects, with export preflight metrics for native animation XML, note-only handoffs, and entrance/emphasis/exit/motion cue mix.
4. Completed collaboration and review hardening for this set with shared-view permission/review snapshots that summarize download mode, unresolved comments, mentions, PPTX comment threads, and note coverage in the view-only surface.
5. Kept Tauri desktop integration on the safe path through existing native menu/media resolver contracts while moving deeper permission-scoped open/save/export APIs, recent-file OS handoff, and desktop regression coverage into the twenty-first feature set.

## Twenty-First Feature Set

Current status: 100 / 100.

1. Advanced deeper ODP import fidelity with flattened multi-series local chart-table conversion, local-table versus linked-data chart preflight summaries, linked-data review warnings, and table-cell style preservation for ODP background fill, text color, bold weight, and border color; richer ODP chart styling, true linked chart-data workflows, and deeper table preset mapping remain.
2. Strengthened collaboration review operations with copyable shared-link review reports in the view-only sidebar, including unresolved comment serialization, mention labels, PPTX thread IDs, slide references, target labels, and download-permission state; deeper owner-visible thread exports, role-aware merge edge cases, and comment-thread handoff planning for PowerPoint export remain.
3. Advanced desktop file integration with permission-scoped native open/save/import/export/recover command payloads, recent-file native handoff metadata, and desktop-safe regression fixtures that avoid broad filesystem access; deeper Rust-side permission-scoped file APIs remain.
4. Improved export confidence with grouped, copyable, and downloadable PPTX preflight snapshots for native master/layout XML readiness, chart/table fallback reasons, media packaging decisions, and known issue details.
5. Added authoring polish for advanced PowerPoint parity with styled action-button payloads, preview swatches, target-slide validation, and regression coverage; richer animation variants, presenter review cleanup, and larger-deck safeguards move into the next feature set.

## Twenty-Second Feature Set

Current status: 100 / 100.

1. Completed the first native Office fidelity slice with real multi-series chart editing through Category/Series TSV data, an explicit `chartSeries` model, multi-series canvas/slideshow preview, native PPTX multi-series export, PPTX/ODP import preservation, server validation, and regression coverage; richer chart styling, stronger table preset mapping, and linked-data review workflows remain.
2. Completed the first permission-scoped Rust/Tauri file API slice with dialog-backed native read/write commands, per-operation extension validation, base64 binary handoff, native JSON save/open/current-file path reuse, native presentation/outline/image-slide imports, native export writes, native recent-path metadata, desktop readiness detection, Rust unit coverage, and TypeScript regression coverage while keeping the default Tauri capability set narrow.
3. Completed collaboration review depth with owner-copyable unresolved-thread handoff exports, role-aware merge/share/export policy summaries, and PowerPoint comment-thread planning that classifies root threads, reply threads, and speaker-note handoffs with regression coverage.
4. Completed the next authoring-control slice with richer animation variants across entrance, emphasis, exit, and motion-path effects, shared option validation, preview/slideshow classes, handoff/report compatibility, and regression coverage; deeper custom action-button styling, presenter review cleanup, and large-deck safeguards continue in production hardening.
5. Completed production readiness hardening with animation-density deck scale metrics, status-bar visibility for animation pressure, and regression coverage for animation-heavy slide overloads; low-cost browser smoke fixtures remain deferred until credentials and machine budget allow.

## Twenty-Third Feature Set

Current status: 100 / 100.

1. Completed deeper action-button authoring with selectable style variants, selected-object slide-jump diagnostics, copyable PPTX action-setting handoff reports, action metrics in PPTX preflight, and regression coverage for grouped button dedupe, external links, and broken targets.
2. Completed presenter review cleanup actions that group rehearsal drift, missing rehearsal, speaker-note polish, missing captions, audience-display setup, and screen-handoff blockers into deterministic workflow rows with jump/use-timing actions and regression coverage.
3. Completed the next native Office export fidelity slice by mapping simple fade-out exit animations into PPTX timing XML, keeping unsupported advanced effects in speaker-note handoff metadata, and adding export-preflight metrics for native exit animations plus PowerPoint comment-thread, reply, and manual-note handoff planning.
4. Completed stronger collaboration operations with role-aware review-thread lifecycle grouping for manual and imported PowerPoint threads, owner/editor/viewer action policy coverage, safer merge summaries, viewer merge blocking, conflict-area grouping, cloud merge review UI wiring, and regression coverage.
5. Completed production readiness hardening with targeted regression coverage for presenter cleanup, native export fidelity, collaboration lifecycle and merge safety, plus a credential-free browser-smoke contract extension for disabled review-only merge controls.

## Twenty-Fourth Feature Set

Current status: 100 / 100.

1. Started deeper native Office export fidelity with a reusable PPTX comment-thread XML plan that groups imported PowerPoint roots/replies, counts manual speaker-note handoffs, reports anchor readiness, feeds Backstage export-preflight metrics, and has regression coverage. Broader action-setting metadata and safer advanced animation XML expansion remain.
2. Added a more explicit native master/layout/theme fidelity plan with `ppt/theme/theme1.xml`, `ppt/slideMasters/slideMaster1.xml`, and `ppt/slideLayouts/*` package-part readiness, reusable theme-file import/export handoff details, placeholder inheritance diagnostics, Backstage preflight metrics, snapshot grouping, and regression coverage.
3. Advanced collaboration operations with richer thread reply controls for manual and imported PowerPoint threads, owner/editor/viewer merge-resolution choices for clean merges and conflicted local/cloud edits, and audit-ready collaboration summaries that serialize review-thread and conflict-choice readiness with regression coverage.
4. Polished desktop packaging with a typed readiness report for file-association planning, OS recent-document registration metadata, signed-package readiness checks, native open/save edge-case coverage, and desktop preflight reporting without broadening Tauri permissions.
5. Completed large-deck and browser-smoke production readiness with stable smoke selectors for status-bar pressure, Backstage export preflight, and desktop packaging status, a broader low-cost browser smoke contract, reusable production pressure diagnostics, joined production-readiness reporting, and targeted regression fixtures for the new feature areas.

## Twenty-Fifth Feature Set

Current status: 100 / 100.

1. Started true desktop registration by moving local deck saves to an app-owned `.essdeck` extension, keeping legacy `.json` decks openable, adding installable Tauri file-association metadata that does not claim generic JSON, broadening scoped Rust/native deck read-write validation, and adding desktop release-registration preflight/reporting for association metadata, OS recent-document handoff gaps, and signing/notarization release gates. The actual OS recent-document writer and release-time signing inputs remain.
2. Deepened native Office action and animation fidelity with reusable PPTX action-setting metadata for slide jumps, URL, mail, and telephone actions; slide-level action handoff notes in PPTX export; native action-setting preflight metrics; and safer animation XML support reporting that separates native-ready effects from supported effects with unstable exported targets.
3. Improved large-deck rendering safeguards with a reusable slide-window and virtual filmstrip contract, status-bar window telemetry, production-readiness diagnostics for active slide windows and canvas detail budgets, and targeted regression fixtures for very large files.
4. Expanded import/export fidelity with a reusable compatibility repair report for linked chart data, chart fallback editability, ODP table preset cleanup, and Office theme-file handoff readiness; Backstage export preflight now threads the latest ODP report into PPTX repair metrics, snapshot sections, and visible repair steps instead of only warning.
5. Completed broader production smoke coverage across auth, share-view download permissions, Backstage recovery/info tabs, desktop file panels, and cloud conflict flows while keeping browser execution optional until stable credentials exist.

## Twenty-Sixth Feature Set

Current status: 100 / 100.

1. Advanced deeper native Office master/theme fidelity with a reusable `.thmx` package-readiness plan, package-part contracts for content types, root relationships, theme XML, and theme relationships, a native master/layout XML authoring task plan for theme package parts, slide-master placeholders, and saved slide-layout placeholders, serialized review notes, PPTX preflight metrics for authoring readiness and handoff counts, and regression coverage. Broader true XML writing remains.
2. Improved ODP and Office table/chart interoperability with a typed repair plan for linked chart data, chart editability fallbacks, ODP table preset mapping, and table structure readiness; PPTX preflight snapshots now expose owner-visible repair actions and interoperability metrics before export.
3. Expanded advanced action and animation metadata with a reusable Office action/animation review plan, safer action catalog classes for slide jumps, URL, mail, telephone, missing-target, invalid-url, and self-targeted actions, richer native/emphasis/exit/motion-path animation handoff classification, and PPTX preflight warnings that map owner actions to exact slide objects.
4. Hardened desktop release readiness with shared release-gate metadata, OS recent-document writer planning, signing/notarization input checks, app-owned file association verification, and Local file panel diagnostics that stay permission-scoped.
5. Added optional production smoke execution readiness with seeded credential checks, mutation-safe prepared fixture checks, targeted browser-flow selection, copyable release-review serialization, and regression coverage while preserving the no-build lightweight verification cadence.

## Twenty-Seventh Feature Set

Current status: 100 / 100.

1. Completed reusable Office theme package XML authoring for `[Content_Types].xml`, root package relationships, `ppt/theme/theme1.xml`, and theme relationship manifests, with preflight metrics, snapshot grouping, serialization, and regression coverage.
2. Completed native slide-master and saved slide-layout placeholder XML authoring for `ppt/slideMasters/slideMaster1.xml` and `ppt/slideLayouts/*`, including footer/date/slide-number placeholders and reusable layout slot geometry.
3. Completed imported PowerPoint comment-thread XML authoring for comment authors, slide comment parts, and slide comment relationships while preserving source thread ids, reply metadata, timestamps, authors, and anchors.
4. Completed safe action-setting XML authoring for slide jumps and URL actions, including transparent click overlay shapes, slide relationship manifests, and Office click-action attributes.
5. Completed advanced native PPTX animation timing XML for stable entrance, emphasis, exit, and motion-path targets, with scale, rotation, fly-out, and motion-path XML coverage plus updated preflight metrics and regression coverage.

## Twenty-Eighth Feature Set

Current status: 100 / 100.

1. Completed the first richer diagram authoring slice with editable Matrix and Pyramid templates, stable diagram group ids that preserve source-template metadata through insertion and duplication, a reusable diagram authoring report, and PPTX preflight/snapshot metrics for PowerPoint-friendly conversion planning.
2. Completed standalone theme/template portability with Essence theme-file export/import payloads that carry paired theme bundles, Office metadata, theme package manifests, and authored XML readiness plus reusable template package manifests that report stale, unused, unpaired, and mismatched-theme assets.
3. Completed collaboration reviewer operations with owner-visible PowerPoint comment-thread export choices, reviewer-safe merge previews, role-aware audit snapshots for shared decks, and review-panel export choices for owner handoff, thread planning, XML authoring, and shared review reports.
4. Completed release automation handoff with CLI-readable text and JSON readiness artifacts for desktop release gates, smoke execution prerequisites, cloud-sync browser contracts, and Vercel/Turso/Better Auth/Brevo/admin environment checks without running production builds by default.
5. Completed desktop/cloud production readiness health with permission-scoped local save/open diagnostics, low-cost smoke fixture planning, admin/auth/deck data health checks, a `bun run production:health` CLI, JSON/text artifacts, and regression coverage while keeping the default flow lightweight.

## Twenty-Ninth Feature Set

Current status: 100 / 100.

1. Completed the first reusable native Office package authoring slice by composing ready theme, slide-master, slide-layout, comment-thread, action-setting, and animation-timing XML into a safe package writer for existing PPTX zip entries, with merge-aware content type, relationship, action overlay, and slide timing handling plus preflight/reporting coverage.
2. Completed desktop OS release polish with a scoped OS recent-document writer for app-owned `.essdeck` files, release-report writer metrics, Local file status surfacing, install-time association verification, signed-package profile checks, and focused Rust/TypeScript coverage that keeps file permissions scoped.
3. Completed production data operations for admin/auth/deck health with a shared production-data snapshot, fixture setup guidance, stale share/deck/session/revision cleanup policies, asset storage hygiene checks, CLI serialization, production readiness composition, and owner-visible admin dashboard remediation rows without exposing secrets.
4. Completed smoke fixture execution depth with mutation-safe deck factories, seeded auth/session fixture review, targeted low-cost flow selection, disabled-by-default optional browser execution handoff, production-health and release-handoff serialization, env-driven fixture metadata, and regression coverage.
5. Completed accessibility and keyboard parity with a reusable focus/shortcut parity report, screen-reader labels for editor landmarks and canvas/layer objects, actionable accessibility diagnostics, visible focus and keyboard-entry checks, shortcut coverage reporting, and regression coverage.

## Thirtieth Feature Set

Current status: 100 / 100.

1. Started deeper Office theme fidelity with a reusable Office theme variant repair report that checks imported source theme review, `.thmx` package XML readiness, master-layout variant availability, placeholder inheritance safety, and owner-visible repair actions in the Master safeguards panel.
2. Completed richer chart/table linked-data review workflows with safe repair actions for unsupported linked chart sources, ODP linked chart data, rotated chart fallbacks, imported table style gaps, and rotated table native-editability handoffs surfaced through PPTX preflight metrics and copyable snapshots.
3. Completed advanced animation and action authoring depth with custom motion-path offsets, on-click/with-previous/after-previous trigger choices, animation-pane and properties-panel controls, slideshow trigger sequencing, native PPTX custom motion-path XML, preflight trigger/custom-motion metrics, and regression coverage.
4. Completed production fixture execution hardening with a shared seeded fixture lifecycle planner, mutation-safe reset/session command reporting, a lightweight `bun run production:fixtures` CLI, production-health and release-handoff integration, disabled-by-default browser handoff serialization, and regression coverage for ready and blocked lifecycle states.
5. Completed app-shell ergonomics with a searchable command palette wired to real slide navigation, insert, object, edit, view, and section commands, a toolbar keyboard trigger, command-prerequisite disabled states, registered smoke selectors, browser smoke contract coverage, and regression coverage for search and command availability.

## Thirty-First Feature Set

Current status: 80 / 100.

1. Started deeper native Office package parity with a reusable Native Office package parity report that joins SmartArt/diagram conversion candidates, `.thmx` theme package application paths, master/layout relationship checks, native package writer readiness, PPTX preflight metrics, snapshot grouping, serialization, and regression coverage; true SmartArt XML conversion and broader presentation relationship writing remain.
2. Completed responsive workspace ergonomics with persisted density and side-panel preferences, collapsible filmstrip/properties rails, compact toolbar density controls, command-palette view commands, smoke selectors, and regression coverage; deeper mobile sheet panels and resizable splitters remain.
3. Completed the configured smoke execution handoff by adding a guarded presentation smoke runner bridge, a reusable browser-smoke runner, a `bun run test:presentation-smoke:bridge` CLI, lifecycle handoff wiring, strict live URL/storage-state/mutation-safe-fixture/approval gates, and regression coverage; live execution remains opt-in only.
4. Completed advanced import/export repair loops with a reusable owner-visible repair report, Backstage copy/download loop panel, linked Excel/ODP chart-data fix actions, rotated chart/table editability steps, ODP table preset mapping actions, final PPTX preflight verification, smoke selectors, and regression coverage; deeper automatic workbook reconstruction and richer ODP-to-Office style transforms remain.
5. Polish presenter delivery with stronger rehearsal workflows, keyboard-first presenter controls, subtitle/caption readiness, audience-window recovery states, and exportable delivery checklists.

## Verification Rule

During feature work, run `bun run typecheck` after meaningful batches. Do not repeatedly run production builds until the requested feature set is complete.
