export interface ProjectVersionActivityData {
  activeCollaboratorCount: number;
  actorEmail: string;
  actorName: string | null;
  actorUserId: string;
  capturedAt: string;
  openCommentCount: number;
  resolvedCommentCount: number;
  totalCommentCount: number;
}
